# 技术债登记表（唯一权威清单）

> 状态：✅ 强制维护。
> 本文件是全项目技术债的 **single source of truth**。
> 各 `docs/archive/mX-audit.md` 只记本里程碑的**动态**（新登记 / 偿还），并指向本表；
> 本表负责**全局汇总**——编号、状态、触发条件、登记里程碑。
>
> 维护规则（见 `milestone-discipline.md` §3）：每个 milestone 收尾审计时，
> 新发现的债追加到本表（递增编号），已偿还的标 ✅ 并注明偿还里程碑。
> **任何文档引用 TD-XX 必须能在本表查到对应条目。**

## 图例

- ✅ **已偿还** —— 已在代码中修复
- 🟡 **开放** —— 已登记，按触发条件处理
- ⚪ **设计性约束** —— 不是债，是刻意的边界（记录以防重复讨论）

---

## 已偿还

| # | 登记于 | 内容（简） | 偿还于 |
|---|--------|-----------|--------|
| TD-1 | M0 | runManager 动态 `await import("./transcript.js")`（本可静态） | M0 当场修 |
| TD-86 | 2026-07-07 dogfood round 4 | 8 个子命令接受 `--format json` 但静默忽略 | **2026-08-14 已偿还（D2，run_20260814164038601neseh1 / commit 4952a27）**：7 个未修子命令（registry validate/check、runs list/summary/grep、wao decision list、wao handoff read）全部落地 JSON 分支，schema 取现有计算结果、text 路径字节守卫（registry validate 角色合同失败行为变化经终审裁决接受并有守卫钉住）、每命令 spawn 级 `JSON.parse` 回归 + wao 两命令新 text 守卫。registry list 此前已修。 |
| TD-109 | 2026-08-14 Lead dogfood | CLI 无终态等待命令（runWait service 存在未接线，U5 平权反例） | **2026-08-14 已偿还（D1，run_20260814151833929rwlupy / commit df42b9d）**：`runs wait <runId>` 委托同一 `runWait()` service（SSOT 边界、semanticNotes 同 selector、CLI 不绑定 workspace、SIGINT 快照+无泄漏、窗口到期 exit 0）；顺带修复 `runs` 未知子命令静默落底为 fail-closed。残留见 TD-114。 |
| TD-110 | 2026-08-14 Lead dogfood | sessionReuse 派发拒绝缺 flag 指引 | **2026-08-14 已偿还（D2）**：`SessionReuseWorkspaceRequiredError` typed error（message 逐字节不变）+ CLI background 路径 catch 后追加静态指引 `pass --cwd <git-root> for sessionReuse agents`；前台路径不解析 sessionReuse（不适用）。 |
| TD-112 | 2026-08-14 Lead dogfood | CLI collect 取最终回复需逆向结构 | **2026-08-14 已偿还（D2）**：`collect <runId> --final` 复用 runCollectProjection compact 路径，四态（available/empty/too_large/cursor 互斥），继承 messages.collected 审计 append；收口包将 too_large 指引改为可照抄的自洽命令。 |
| TD-111 | 2026-08-14 Lead dogfood | 认证 advisory 缺拒绝原因与新鲜度上下文 | **2026-08-14 已偿还（D3，run_20260814183307740po1kkz / commit 80f1262）**：`certificationReasons.js` 冻结闭集 SSOT（加载期自检）+ `reasonCodeFor` 纯映射；旧自由文本 `reason` 原文不动；三层数据（case `lastHealthyRunAt` → summarizeWorkers 聚合 → 投影并列字段，legacy 投影 null）；MCP 双 schema enum 同源派生；blockerReason 泄漏面经终审三重诱饵测试确认闭死；真实 `--agent coder_low` 单跑端到端验证（certified→null、rejected→core_checks_failed）。残留语义见 TD-115。 |
| TD-2 | M0 | waitForCompletion 与 abort 双调 onRemove 无幂等保护 | M0 当场修（`_removed` flag） |
| TD-3 | M0 | backend.abort 失败时 catch 不写 state_change（内存/transcript 不一致） | M0 当场修（catch 也 transition） |
| TD-107 | 2026-08-01 M12-7 closeout | canonical 测试夹具依赖本机状态、Windows 锁/墙钟假红、资源类型无差别并发，导致冻结候选随机失败且只能整套重跑 | **2026-08-01 已偿还**：唯一 `npm test` 入口由 `scripts/canonical-test.mjs` 按 tracked manifest 分波调度；registry/`.wao` fixture 自包含，锁与计时用确定性注入，首轮失败保持非绿并只做一次隔离归因，结构化报告区分 stable failure / isolation pass / environment invalid。真实本机配置继续由 operation acceptance 验证。 |
| TD-119 | 2026-08-15 Lead dogfood（friction log `2026-08-15-collect-projection-long-message.md`；Owner 批准升级并处理） | collect 投影页级 `truncated` 语义误导 + 长消息分块交付不可发现：full 模式对长消息实为**无损分块交付**（≤4000 字符/entry，单页预算内拼接入全文，游标本就支持消息内续读），但页级 `truncated` 把"切片交付"与"内容扣留"混为一谈——`truncated:true + nextCursor:null` 自相矛盾，Lead 读成"内容丢失且取不回"（当日实证 3 次，被迫走 `tail` 通道）；`--final` too_large marker 的 fallback 指引未告知分块拼接语义。 | **2026-08-15 已偿还（含三方会审返工）**：定性为**已成文契约变更**（usage.md 边界句原承诺"超限设 truncated:true"）而非纯 bug 修复。(a) `runCollectProjection.js` 页级 `truncated` 改为 withheld-only（=== hasMore，必伴 `nextCursor`；entry 级 `truncated:true` 保留为切片标记，**entry 级 `truncated:false` = 消息边界**——多消息同页按此切分，勿整页盲拼）；(b) `--final` too_large marker 补分块拼接+消息边界指引（observe.js / usage.md / collectFinal.test.js 三处同步）；(c) usage.md：边界句重写为新语义 + 续读段补"单页内分块交付"与消息边界规则 + compact too_large 补 full 模式指引（MCP 面不再是无指引死路）+ 反回归守卫（旧句"超限设 truncated:true"禁止复现）；(d) mcpRunCollect.test.js +5：9615 单页无损分块、15000 跨页 withheld 必伴 cursor、恰 4000/恰 12000 预算边沿锚点、多消息同页边界规则复原。既有 12 消息封顶用例（真扣留）行为不变。 |
| TD-114 | 2026-08-14 D1-D3 终审（auditor/coder_mm 双审） | 测试覆盖尾巴 | `runs wait` 窗口到期（terminal:false）的 exit 0 缺子进程级退出码断言（SIGINT 已有守卫；当时仅源码可推 + 假时钟 W2 间接覆盖）。 | **2026-08-15 已偿还（Round2-AB，run_20260815215057671x9egen / commit e5a743a + 验收一行级强化）**：进程级"窗口到期 exit 0"**未直接实测**——被 `RUN_WAIT_MIN_MS=180000`（runWait.js:32）确定性排除（3 分钟真实等待违反套件纪律，假时钟无法跨进程注入；TD-114 原文授权进程内替代），由组合覆盖：B-1 进程内（真服务+假时钟，json/text 各一）断言窗口到期不改 `process.exitCode`（save/restore prev）+ B-2 子进程终态路径 exit 0 + "Terminal: yes"（复制 W3c 的 WAO_SKIP_VERSION_GUARD 注入；终态与到期共享同一 return/print 代码路径）。验收：coder_mm + coder_hq 双席 PASS（独立重跑）。 |
| TD-121 | 2026-08-15 TD-119 三方会审（coder_low "Lead 没问但应该问的问题"） | collect 页级 `truncated ⟺ (nextCursor !== null)` 不变量未在 MCP 边界强制——投影层以下的回归会透传到 wire 而非 fail-closed | **2026-08-16 已偿还（Round3-B，Owner 采访裁定同轮纳入；实施 coder_hq，run_202608160044287189qgair / commit 1d382bc，验收 coder_mm + coder_low 双席 PASS：run_20260816005459129ummkj6 / run_20260816005508690jr1syd）**：(a) Owner 2026-08-16 裁定同轮（原触发条件"触碰 schema/投影回归实证"未被严格满足）；(b) 对登记文本"schema 层"的偏离为 **handler 级投影器检查**——`RUN_COLLECT_OUTPUT` 定义处显式禁用 superRefine/discriminatedUnion（SDK 兼容风险），故循 projectList 先例：`server.js` 导出纯函数 `collectPageConsistencyViolation`（严格布尔比较 + 空串游标双配对显式拒绝 + 错型/非对象/缺字段 fail-closed，entry 级 `messages[].truncated` 不敏感）+ 新 seam `projectCollectResultFn`（M12-19 `resolveWorkspaceBindingFn` 同款），检查位于 parse 与 `commitAppend` 之前——违规折叠固定 `"run_collect failed"` 且零 audit append，fail-closed 语义等价；m12-10 三冻结常量逐字节不变 = 零 wire 漂移证明；测试四组：纯函数四配对 / 错型与非对象 / 双向不一致边界因果（`commitCalls===0`）/ 同 seam 一致反例（audit 提交 + drilldowns 仍在）。(c) CLI `collect --format json` 同投影无 handler 检查——**有意不覆盖**（TD-121 登记文本限定 MCP 边界；CLI 消费者为 Lead 本人且 JSONL 直读兜底）。(d) WQ-02 状态清单：一致对（两向）过；不一致对（两向）fail-closed；错型（`truncated:"false"/0/1`、`nextCursor:7/""`）fail-closed；缺字段/非对象 fail-closed；compact 恒一致（runCollectProjection 恒 false/null）过；entry 级切片标记按设计忽略；stale-data N/A（检查作用于内存投影结果，读失败已在前置 catch 收敛）。集成后全量 `npm test` 207/207。**残余（验收双席一致声明，触发式加固候选）**：readiness×candidateKind 配对正确性未机器化（Round3 关系型守卫只断言成员在场）；`TS_RUNBOOK_ALLOWLIST` 私加仅评审可拦。**伴随项**：下次触碰 runDelivery 投影时补 backend_failed↔ambiguous 的 readiness 标签断言测试。 |
| TD-122 | 2026-08-15 P1 layering 验收会审（auditor，run_2026081514383395251gx32） | 分层白名单是**可消除的例外**：sessionReuse.js 归 core 则白名单归零。 | **2026-08-15 已偿还（Round2-C，run_20260815220227976k7c9qr / commit 243cf70b）**：`CORE_MEMBERS` 精确成员例外（对称于 SHARED_MEMBERS 既有机制）使 `src/application/sessionReuse.js` 归 core 桶——classifyPath application 分支三分；`WHITELIST = Object.freeze([])`（upward 2→0，双向 deepEqual 空集；机制保留：HARD_CAP 12 + 回填契约"先改 docs SSOT"）。有牙三重：纯函数档（classifyPath==="core" + 合成 registry→sessionReuse 零违例）、真实树 deepEqual []、docs-consistency CORE_MEMBERS 切片解析断言。docs 五处同步（:50/:56"恰 0 条"/:80/:114 前缀保留/:1125）。附带事实修正：sessionReuse 头注释原列 claudeCode 为第 5 消费者系既有错误（无 import 边，只经 spawn args 间接翻译），更正为 runContinue.js，5 消费者数量不变。验收：coder_mm + coder_hq 双席 PASS。 |
| TD-48 | 2026-06-26 M8；2026-08-11 真实触发 | 历史 run 全量解析曾使 MCP `lead_preflight` / `runs_list` 冷查询明显变慢，并怀疑 CLI 查询存在同类性能缺口 | **2026-08-13 post-M12 关单**：MCP 查询面已由 M12-18 的 metadata-validated process-memory LRU 偿还；本轮实测确认 CLI `runs diagnose` 只读取 single run transcript（约 0.45–0.98s），`runs dashboard` 在 1887 runs / 约 180MB 上全量扫描约 1.84–2.48s，未达到既定 5s 触发条件，因此不新增缓存或索引。若数据规模或 watch 放大使查询稳定超过 5s，再以新事实登记。 |
| TD-71 | 2026-06-27 测试稳定性；2026-07-13 再捕获 | Windows 上创建 transcript append lock 偶发 `EPERM`，同一工作树隔离运行通过 | **2026-08-13 已偿还**：append-lock 只在 `fs.open(..., "wx")` 的 `EPERM` / `EBUSY` 上按既有 5000ms deadline 与 5ms poll 做有界重试；`EEXIST` 独占 stale-lock 处理，其他 open 错误立即抛出。open 成功后的 metadata write 失败只关闭并删除自己持有的 lock，再抛原错误；确定性注入测试覆盖 retry、deadline、非瞬时错误和 cleanup ownership。 |
| TD-80 | 2026-07-02 codex e2e | legacy transcript 在 backend 非零退出前已有有效 command/file 证据，却因缺少后来的 `run.evidence_audit` 而被诊断为普通 backend failure | **2026-08-13 已偿还**：仅当 transcript 完全没有 audit event 时，diagnosis 才用共享 `assessRunEvidence` 对 legacy 事实做安全回退；显式 audit（含 failed/malformed）始终优先，assistant/tool-only 不会提升。真实历史 run `run_20260702142549160dfqmrt` 只读回放得到 `evidence_passed_backend_failed`，不回显 command/path/provider 原文。 |
| TD-6 | M1 | OpenCodeServeBackend 旧 waitForCompletion 死代码（含 2 测试） | M1 删除 |
| TD-7 | M1 | runManager 末尾 sleep 死代码 | M1 删除 |
| TD-8 | M1 | resume 里第二处动态 import（TD-1 同模式漏改） | M1 当场修 |
| TD-9 | M1/M2/M4 | RunEvent 证据链事件（command/file_written/tool_use/tool_result）未实现 | **M6** 全部实现 + claude/codex 两个 backend 提取 |
| TD-10 | M1 | 进程式 resume 不可能（TypeError） | M3-5 重放路径 |
| TD-11 | M2 | ProcessBackend 模块级 resolveWait 跨 run 共享（作用域 bug） | M2 重构为 EventQueue 类（实现中自检发现） |
| TD-14 | M2 | resume 硬编码 opencode 方法 | M3 按 backend 类型分支 |
| TD-17 | M2 | codex 在 Windows 是 .cmd 包装器，spawn EINVAL | M2 覆写 resolveBinary 直调 codex.js（**真实 smoke 才暴露**） |
| TD-18 | M2 | opencode 流式 parts 竞态（emit 不完整 message） | M2 改稳定性判定（看到 assistant 再确认快照不变才 emit） |
| TD-19 | M3 | createWorktree 返回 Promise 但 start 没 await | M3 当场修 |
| TD-20 | M3 | mock getAgent 的 `...overrides` 不过滤 undefined | M3 当场修（mock 对齐真 registry.js） |
| TD-25 | M5 | router activeRoutes 在层结束时清除（影响下一层） | M5 改 pendingRoutes（**真实 smoke 才暴露**） |
| TD-26 | M5 | _log payload 字段覆盖 transcript context 同名字段 | M5 自动重命名冲突字段（**真实 smoke 才暴露**） |
| TD-35 | 事故修复 | **会话兜底 abort 缺失（真实事故）**：RunManager 的 completed/failed/timed_out 路径都不调 handle.abort，opencode-serve 的 HTTP session 在 run 结束后可能继续活着。对无限多轮模型（DeepSeek-v4-flash）是 quota 黑洞——一次 researcher run 超时后 serve session 未被 abort，烧光用户当日 quota。 | 2026-06-17 修复：`_runCleanup` 兜底调 `handle.abort`（幂等 `_sessionKilled` flag 防 user-abort 路径重复），3 红绿测试覆盖 completed/failed/timed_out。注意：这只偿还“缺少 abort 调用”，后台 quietness 验证另见 TD-37。详见 `docs/incidents/2026-06-17-deepseek-quota-drain.md` |
| TD-37 | 2026-06-18 GLM quota 事故 | opencode `stop`/abort 只验证本地 ledger，不验证 serve 端 session 是否真停；06-18 事故实证会导致后台继续烧 token | 2026-06-26 主体清零，2026-08-02 收紧共享 server 边界：`stopCommand` abort 后做 bounded polling（status/token/message 增量比对）；quiet=false 或观测不可用写 `run.stop_unverified` + 告警，不对可能承载无关 session 的共享 OpenCode server 做全局 taskkill。reliability stop drill 真实 provider 端到端通过，`backendStopQuietVerified` 有证据。详见 `docs/incidents/2026-06-18-glm-quota-drain.md` |
| TD-38 | 2026-06-23 Safety Gate | 可靠性 | `_runCleanup` 兜底 abort（TD-35）不做静默验证，可能虚假成功 | 2026-06-24 修复（C6）：`_runCleanup` abort 后调 `_verifyStopQuietIfCapable`——handle 有 session/messages（opencode 类）则复用 verifyStopQuiet 验证后台静默；未停写 `run.stop_unverified` + 告警。进程式 backend 自动跳过验证。与 TD-37 + S1-1 共同构成 opencode 三层防线 |
| TD-39 | 2026-06-23 外部审计 P0 | 可靠性 | 最小护栏（拒绝裸 spawn）只是"拒绝脚枪"非"关洞"——fire-and-forget 仍可造孤儿 session | 2026-06-25 修复（M7-P2）：新增 `src/backgroundRunner.js`（detached runner 进程）+ CLI `run/spawn --background`。CLI 不再拒绝裸 spawn，而是 fork detached runner 托管（token 闸门/超时/兜底 abort 全生效），不再产孤儿 session（06-18 架构洞正解）；顺带根治 F1。RunManager 旧护栏保留作深度防御。详见 `docs/archive/m7-phases.md` P2 |
| TD-40 | 2026-06-23 外部审计 P2 | 可靠性 | Node v24 有 libuv Windows Job Object 回归（杀长进程）；架构 spec §4.3 想要的 Job Object 绑定未实现 | 2026-06-26 修复：**重定义**为"在零依赖约束下复用 Node 内置 Job Object（v22）+ engines/启动校验守住"。行业调研结论——业界最佳实践 = taskkill /T/F（WAO 现状）+ 复用内置 kill-on-job-close，**无主流包用自定义 Job Object**（需 native binding，违反零依赖）。新增 `src/nodeVersionGuard.js`（v22 放行/v24 全拒/数据驱动未来修复版放行）接入 cli+daemon+backgroundRunner 三个 spawn 入口；`engines.node: ">=22 <25"`；保留 taskkill /T/F 作主动 abort。测试用 `WAO_SKIP_VERSION_GUARD=1`（`npm test` 已注入）。详见 ADR 0013 |
| TD-33 | M6 | opencode-serve 证据提取未实现（schema 已在 `research/07` 勘测完毕，待落 parser） | 2026-06-18 修复：`opencodeServe` 从 tool part 抽取 command/file_written/tool_use/tool_result evidence；后续 runtime+model 组合用 `npm run reliability -- --profile strict` 做 live 认证 |
| TD-36 | runtime certification | Claude Code 外部 `.bat` wrapper 用裸 `%*` 转发参数，prompt 中的 `<...>` 被 `cmd.exe` 当作重定向二次解析，导致进程 exit 0 但 stdout 为空，WAO 只能看到伪完成 | 2026-06-18 修复：新增 `agent.prependArgs` + `scripts/wrappers/claude-code-provider-wrapper.mjs`，用 Node `spawn(binary, args)` 数组转发参数；DeepSeek via Claude Code wrapper 通过 strict reliability matrix |
| TD-27 | M5 | 错误处理 | 并行层 handler 抛错（非 backend 错误）让 Promise.all reject 整个层 | 2026-06-23 修复：engine per-node try/catch，handler 抛错节点记 `completed:false + error`，失败传播照常走，兄弟节点不受影响。TDD 红绿覆盖 |
| TD-30 | M5 | 超时 | 无 workflow 级整体超时（只有单节点 waitTimeout） | 2026-06-23 修复：`execute()` 支持 `options.workflowTimeout`，每层开始前检查 deadline，超时截断剩余层并返回 `{completed:false, timedOut:true}`。不传则 Infinity（向后兼容） |
| TD-12 | M2 | 去重 | claude stream-json 同 message.id 分多条到达，不去重 | 2026-06-23 修复：`ClaudeStreamParser` 维护 `_seenMessageIds` Set，重复 id 的 assistant 行跳过；无 id 不去重（向后兼容） |
| TD-28 | M5 | 测试 | router 下游同时有 dataEdge 边的行为未测 | 2026-06-23 补测试：router 选中的下游同时通过 dataEdge 接收数据，验证路由过滤 + 数据传递都生效（行为本就正确，纯覆盖缺口） |
| TD-32 | M6 | 测试 | scorecard 并发证据未测（多条 command 同时到达，evidence 数组顺序） | 2026-06-23 补测试：多条同命令不同 exitCode（锁定 find 取首个的顺序依赖）+ tool_result 乱序到达按 toolCallId 关联推断 exitCode（含 isError=true 失败路径） |
| TD-5 | M0 | 测试 | 并发场景未测（多 run 同时 wait/abort） | 2026-06-23 补测试：两 run 并发 wait 交叉 abort（验证 activeRuns 隔离 + _aborted flag 独立）+ 同 manager 内并发一超时一完成 |
| TD-22 | M3 | 命令缺失 | persistent worktree 累积，无 worktree 管理命令 | 2026-06-24 修复：新增 CLI `worktree list` / `worktree remove <path>`（能力层 listWorktrees/removeWorktree 早已实现，补 CLI 暴露 + help）。2 红绿测试覆盖 |
| TD-21 | M3 | 偶发 | worktree remove Windows 偶发 Permission denied（已加 fallback，测试偶发 ~1%） | 2026-06-24 修复：removeWorktree 加 3 次重试 + 线性退避（100/200ms），rmSync fallback 后检查 existsSync 确认真删；幂等/容错测试覆盖（删两次/删不存在路径不抛） |
| P1-1 | 2026-06-23 外部审计 P1 | 可靠性 | **✅ 已实现 + 测试守卫（opt-in 强制门）**：审计 P1——dispatch 路径不校验 worker 是否在新鲜 reliability-summary 里。修复：RunManager.start 支持 `requireCertified`（opt-in，默认关），启用时读 `runDir/reliability-summary.json` 校验 ①worker 在 summary ②status ∈ {certified,conditional}（core 全过即放行）③记录身份与 registry 一致 + 该 worker 的 lastHealthyRunAt 在 certFreshnessDays(30) 内（TD-131/TD-132 起；原为全局 generatedAt 判据），不满足则 spawn 前拒绝 + 指引。CLI `run --require-certified` 暴露。06-18 事故教训"调度安全不能建立在模型行为假设上"。**2026-06-25 补 8 红绿测试守卫全部路径**（默认关/缺summary/worker缺失/rejected/certified/conditional/manualOverride/过期），门控逻辑不再无守护。认证重跑已闭环（见 TD-42） | ✅ 门已落 + 8 测试守卫；认证闭环见 TD-42 |
| TD-42 | 2026-06-24 配置验证 | 验证 | agents.example.json 对齐决策 0005 后，3 个新配置未经 probe 验证。**2026-06-25 闭环**：全量 reliability 重认证 + probe 实测全部验证——① coder_low `glm-5-turbo`=certified ② `ZHIPU_API_KEY` env 名正确（coder_low/coder_hq 双双 certified）③ auditor 配置：`opus-4.8` 是无效格式（claude-code 2.1.187 只认别名 `opus` 或全名 `claude-opus-4-8` 连字符），改 `claude-opus-4-8`+`--effort xhigh` 后 certified。6 worker 全认证（5 certified + coder_mm conditional） | ✅ 2026-06-25 全部 probe + 认证验证闭环 |
| TD-43 | 2026-06-25 安全 | **进程式 backend silentTimeout 断路**：RunManager 算出 silentTimeout 传给 `handle.events(signal,{silentTimeout})`，但 processBackend.events 签名是 `(signal)` 第二参数被丢弃——进程式 worker 静默死循环（provider 529 重试 / 白名单 / 不存在的 model，进程活着但 parser 零产出）只能干等 waitTimeout，期间烧 token 占资源。是 opencode 三层防线（TD-37/38/39）之外、进程式 lane 独有的静默缺口 | 2026-06-25 修复：processBackend.events 接第二参数，_streamEvents 复用 opencodeServe 语义——silentTimeout 内无任何 parser 事件则 `doneEvent(failed,'silent timeout')`+kill。3 红绿测试覆盖（静默触发/有响应不误杀/不传则向后兼容） |
| TD-44 | 2026-06-25 运维 | reliability 套件 WAIT_TIMEOUT 默认 120000，strict profile 含 scorecard+isolation+workflow 多 drill，120s 在重 worker（codex/claude-code strict）上卡边界，全量批跑易 timeout | 2026-06-25 修复：默认调到 300000（`--wait-timeout` 本就支持覆盖）。milestone-discipline §6.7 补运维说明（单 worker 增量合并 + 超时） |
| TD-54 | 2026-06-26 二次 dogfood | `run/spawn --background` 参数透传不完整会生成 ghost runId（默认 registry/runDir 未透传、`--prompt-file` 未进入 runner、启动失败无 transcript） | 2026-06-26 修复：background 分支先 `loadPrompt()`；detached runner 默认接收 `config.registry`；`runBackground` 预生成 runId 并在启动失败时写 failed transcript（含 `prompt.sent`/`run.error`）；help 补 `run --prompt-file`；`test/cli.test.js` + `test/backgroundRunner.test.js` 覆盖 |
| TD-55 | 2026-06-26 二次 dogfood | 手动 stop 与 wait 路径竞态破坏终态/seq（aborted 后被 failed 覆盖，seq 回退，diagnose 误归 crash） | 2026-06-26 修复：`JsonlTranscript.append` 加文件锁并按磁盘最大 seq 续写；`waitForCompletion` 写终态前尊重 transcript 既有 terminal state；stop 路径写 `run.aborted`；diagnose 前置识别 stop/aborted；`test/transcript.test.js`/`test/runManager.test.js`/`test/diagnosis.test.js` 覆盖 |
| TD-56 | 2026-06-26 二次 dogfood | 活跃 tool/command/metrics events 不触发 running 状态，主控轮询误读为未启动 | 2026-06-26 修复：`waitForCompletion` 对首个 message 继续用 `first_message`，对首个 metrics/tool/command/file evidence 统一转 `running(first_event)`；`test/runManager.test.js` 覆盖 |
| TD-57 | 2026-06-26 二次 dogfood | `wao handoff write --to lead` 与 `wao handoff read lead` 寻址不对称，worker→lead incoming 读不到 | 2026-06-26 修复：`readHandoff` 扫描 handoff 正文 heading `from → to (ts)`，按 `to === role` 过滤并取最新 incoming；CLI help 明确 latest incoming；`test/waoHandoff.test.js` 覆盖 |
| TD-58 | 2026-06-26 二次 dogfood | `AGENT_ONBOARDING.md` 最小闭环示例引用已不存在的 `coder_strict` / `coder_glm_claude` | 2026-06-26 修复：onboarding 改为 `coder_low` 最小闭环、显式 `--cwd <目标项目>` + `--registry <WAO目录>/config/agents.json`，补 runtime skill 目录安装说明并消除重复 `## 4`；`test/docs-consistency.test.js` 覆盖 |
| TD-52 | 2026-06-26 M8 | help 与代码漂移：`printHelp` 漏列 `runs dashboard/diagnose/forecast`、`wao` 族、`daemon` 族 | 2026-06-26 修复：补全 printHelp + spawn-based help 守卫测试，见 `test/cli.test.js` |
| TD-53 | 2026-06-26 M8 | `run --format json` 在 scorecard 注入前 early-return，导致 JSON 输出无 `scorecard` 字段 | 2026-06-26 修复：scorecard 注入前置于格式分支之前，json/text 两路都带；`test/cli.test.js` 覆盖 |
| TD-59 | 2026-06-26 fresh-runtime handoff WF-1 | claude-code OAuth 登录态会覆盖 provider wrapper 注入的第三方 provider key，导致 researcher/coder_hq/coder_low 对 DeepSeek/ZHIPU 401 | 2026-06-26 修复：`claude-code-provider-wrapper.mjs` 为 provider worker 设置隔离 `CLAUDE_CONFIG_DIR=.wao-worker-claude-config/`，删除 OAuth refresh hint，保留 `ANTHROPIC_AUTH_TOKEN` provider key 注入；auditor 不走 wrapper，不受影响。`test/claudeProviderWrapper.test.js` + docs-consistency 覆盖 |
| TD-60 | 2026-06-26 fresh-runtime handoff WF-2 | `run/spawn --background --scorecard-rules <非法 JSON>` 会先返回 runId，detached runner 随后解析失败退出，形成无 transcript 的 ghost run | 2026-06-26 修复：`spawnBackgroundRunner` fork 前复用 `parseScorecardRules` 做 fail-fast，非法 JSON 在可见 CLI 进程内非零失败且不打印 runId；`test/cli.test.js` 覆盖 |
| TD-61 | 2026-06-26 fresh-runtime handoff WF-3 | `wao doctor` 只检查 provider key 存在，不提示 Claude OAuth 登录态可能覆盖 provider-wrapped worker 的第三方 key | 2026-06-26 修复：doctor 读取 `~/.claude/.credentials.json` 的 `claudeAiOauth`，并在 registry 存在 provider-wrapped claude-code worker 时输出非失败 WARN；auditor-only 不触发；`test/cli.test.js` 覆盖 |
| TD-62 | 2026-06-26 fresh-runtime handoff WF-4 | PowerShell 经 `npm run cli --` 传 inline `--scorecard-rules` 容易损坏 JSON，且缺少文件型规则入口 | 2026-06-26 修复：新增 `--scorecard-rules-file`，foreground/background 统一从文件加载并复用 scorecard JSON 校验；help 与 SKILL 示例改为文件方式；`test/cli.test.js` + docs-consistency 覆盖 |
| TD-63 | 2026-06-26 fresh-runtime handoff WF-5 | `runs scorecard --format json` 在无 `scorecard.checked` 时输出纯文本且误报“无规则”，无法区分未配规则与提前失败 | 2026-06-26 修复：无结果分支尊重 `--format json`，输出 `{scorecard:null, reason:"no_rules"|"failed_before_scorecard"}`；RunManager 在 `run.started` 写 `scorecardConfigured` 供判别；`test/cli.test.js` 覆盖 |
| TD-64 | 2026-06-26 fresh-runtime handoff WF-6 | `run --background` 返回 runId 后立即 `status <runId>` 可能因 transcript 尚未创建而 ENOENT | 2026-06-26 修复：`spawnBackgroundRunner` fork detached runner 前同步写 `run.background_submitted` + `pending` 状态 transcript，返回 runId 时 `status --format json` 已可读；`test/cli.test.js` 覆盖 |
| TD-65 | 2026-06-26 fresh-runtime handoff WF-7 | `status` 实际输出 JSON，但 help 未暴露 `--format json`，首次使用者无法确认机器可读契约 | 2026-06-26 修复：help 补 `status <runId> [--run-dir DIR] [--format json]`，并由 help 守卫测试覆盖 |
| TD-66 | 2026-06-26 fresh-runtime handoff WF-8 | `registry list` 的 model 列对 kimi-code/codex 默认配置显示 `-`，选型时误读为缺模型信息 | 2026-06-26 修复：提取 `displayModel()`，按 `model`/`provider.model`/args/prependArgs flags 取显式模型，进程式 backend 缺省时显示 `(default)`；`test/cli.test.js` 覆盖 |
| TD-67 | 2026-06-26 fresh-runtime handoff WF-9 | `runs dashboard` 固定 `RUN_ID` 列宽，长 runId 会把后续列整体推歪 | 2026-06-26 修复：dashboard 先构造表格行，再按本批数据动态计算列宽；长短 runId 对齐测试覆盖 |
| TD-68 | 2026-06-26 fresh-runtime handoff WF-10 | README/SKILL/AGENT_ONBOARDING 对 `registry list` / `registry check` / `registry validate` 三命令分工不一致，首装用户会犹豫该跑哪个 | 2026-06-26 修复：三份入口文档统一短锚点：`registry list = inventory + certification status; registry validate = static schema; registry check = live opencode health`；docs-consistency 覆盖 |
| TD-69 | 2026-06-26 reliability gate | `scripts/run-reliability.mjs` isolation drill 调用 `execFileSync` 但未从 `node:child_process` 导入，导致真实 reliability gate 中 researcher core/strict 过但 operational 误失败 | 2026-06-26 修复：补 `execFileSync` import，并在 `test/reliabilityCertification.test.js` 增加静态守卫；随后 `npm run reliability -- --agent researcher` 与 `--agent coder_low` 均通过 |
| TD-70 | 2026-06-27 flaky test ticket | Windows 全量并行下 background/detached CLI 测试清理临时目录时，detached runner 仍可能短暂持有 cwd/file handle，导致 `rmSync(..., {recursive:true, force:true})` 抛 `EPERM`/`EBUSY`/`ENOTEMPTY` flake | 2026-06-27 修复：`test/cli.test.js` 增 `rmrfRetry()` 同步重试 helper，并将 background/detached 相关临时目录 cleanup 切到 retry；新增 Windows cwd-hold 回归测试；验证命令见本次工单记录 |
| TD-72 | 2026-06-28 fresh-agent 入口流程 | 两个互锁缺口：① `wao doctor` 把 `wao_init: 未初始化` 计为 FAIL（exit 1），但 doctor 是 onboarding §4d 的 preflight 第一道——"未 init" 是 `run wao init` 之前的正常初态，不该与 401/key 缺同列让 fresh-agent 第一步误判环境坏；② **SSOT 铁律盲维**：三条铁律 + docs-consistency.test.js 守的全是"内容去重"（状态机/事件表/角色），**命令调用形式一致这一维从未被铁律或测试覆盖**，导致 AGENT_ONBOARDING.md 全文 8 处用 `node <WAO>/src/cli.js`（v24 下被 version guard 拒），而正确的 `npm run cli`（v22 shim）从未出现在任何面向 agent 的入口文档——fresh-agent 照抄 onboarding 在第一步 `wao init` 就被挡，且文档没告诉他正确入口。 | 2026-06-28 修复：(a) doctor `wao_init` 改三态——已初始化 OK / 未初始化 WARN（不计入 HEALTHY 判定，exit 0）/ 结构异常 FAIL（回归保护）；新增 never-inited 与结构损坏两测试。(b) AGENT_ONBOARDING/SKILL/usage/troubleshooting/AGENTS 的裸 `node .../src/cli.js <真命令>` 统一切到 `npm run cli --`，保留"不要直调"解释注。(c) **核心**：docs-consistency 增"命令调用形式一致"护栏测试（扫描 6 份入口文档，禁止裸 `node .../src/cli.js <真命令>`，白名单 help/explanatory）——把"命令形式一致"从 prose 铁律变成机器不变量。元教训：凡依赖 agent 自觉遵守 prose 的约束都会漂，落到确定性测试里才守得住。`test/cli.test.js` + `test/docs-consistency.test.js` 覆盖；真实 fresh-agent e2e 闭环（coder_low）通过。 |
| TD-73 | 2026-06-28 friction log 收口 | auditor 使用缺成本闸门：SKILL 旧把 auditor 列为责任链默认一环，无"何时跳过"反向闸门，Lead 拿确定性证据仍多跑 auditor 烧 token（tester run 459s/2.65M tokens 仍触发）。证据：两份独立 friction log（WAO core + `<目标项目>_gui` FL-7）独立报告同一现象，指向机制缺口。 | 2026-06-28 Lead verdict 落地：SKILL §"Auditor 调用 policy"——默认跳过 auditor（自审+scorecard 优先，不确定才加）；**无硬红线**（任何场景 Lead 可跳过并对决策负责）；前置/后置 auditor 各自独立判不预设优先级。守住 🟡 边界：policy 落成 SKILL 显式规则但不自动化替 Lead 决定。证据素材见 `.dev/friction-log/2026-06-28T17-00_wao-and-gui-friction-log-merged.md`（auditor -SkipBackend 拦截作反向证据保留）。 |
| TD-74 | 2026-06-28 codex e2e | 诊断精度 | diagnosis 缺 provider 流式中断识别：无 `provider_disconnect` 类，"中途静默+exit 1+无 result"被笼统归 crash（runtime 真崩），与"provider 偶发断流可重派"混为一谈。证据 `run_2026062818401405116u1yd`（coder_hq/GLM-5.2）：死前 84 事件 + 末段静默后 exit 1，metrics=0，属 GLM 网关流式中断。 | 2026-06-28 落地（与 TD-75 合并）：`src/diagnosis.js` 新增 `provider_disconnect` 类（排在 crash 之前），**Lead verdict 判据**=死前 last run.event 距 run.error ≥120s + ≥3 run.event + state=failed + 无 completed，保守（宁漏贴勿误贴），不满足落回 crash。**未改 processBackend 兜底**（exit≠0→failed 结论对，改了削弱真崩检测）。test/diagnosis.test.js +3 测试。 |
| TD-75 | 2026-06-28 codex e2e | 可观测性/Lead 契约 | worker 心跳暴露给 Lead：此前判 worker "活没活"只能猜静默时长，易误判停一个还在思考的 worker（"打架内耗"）。缺一个只反映"worker 在产出"的轻量信号（不存 thinking 内容，token/隐私成本）。 | 2026-06-28 落地：`status` 加 `lastActivityTs` + `secondsSinceActivity`（= 最后一条 run.event 的 ts 及距今秒数；appendFile 每 event flush 故实时）。**只看事件流不引 isAlive**（Lead 选）。SKILL §轮询节流升级：secondsSinceActivity<120 还活着别动（守宁慢勿杀），≥120 才判停/重派；**判停永远 Lead 决定，WAO 不自动停**。test/cli.test.js +3 测试。未做：thinking 内容进 transcript（独立大议题）。补全：lastActivityKind + lastActivitySummary（message→在说话 / command→跑命令 / tool_use→用工具X / file_written→在写文件）。 |
| TD-76 | 2026-06-28 thinking 信号调研 | 可观测性 | **✅ 已落地（2026-06-28，方案 A：只记存在不存内容）**：thinking 期间 transcript 假死（worker 思考时无 run.event）是 TD-74 provider_disconnect 误判的根因。调研证实可行：claude-code stream-json 把 thinking 块放 `obj.message.content` 数组（decision 0009），流经 claudeCode.js:51 但被 `filter(c=>c.type==="text")` 丢弃。**真实 schema 实证**（GLM-5.2 网关实测，非臆测）：`{type:"thinking",thinking:"<内容>",signature:""}`，thinking 与 text 分两条 assistant 行（共享 message.id）；**GLM 确实吐 thinking**（推翻 WebSearch "可能抑制"顾虑）。 | 2026-06-28 落地分步：① 加 raw-capture 机制（`WAO_RAW_CAPTURE` env / `rawCapturePath` 构造参数，旁路日志，不影响 transcript）；② 真实 coder_low run 拿 schema（GLM 确认吐 thinking）；③ `runEvent.js` 加 `thinkingEvent()` + `claudeCode.js` 捕获 `type==="thinking"` emit 心跳事件（**不存内容**，零 token/隐私成本）；④ `cli.js` describeActivity 映射 thinking→"在思考"。心跳思考期间持续，消除假死。**codex 后端仍无 thinking 信号**（parser 无 thinking item），其静默仍按 ≥120s 判——留观察。test/processBackend.test.js +3。 |
| TD-77 | 2026-06-28 codex e2e（双子项） | 失败 run 信息抢救 | **✅ 已落地（2026-06-28，双子项 A+B）**：失败 run "崩溃后两手空空"是同一用户体感的两条腿——`collect` 只重建 message（A）+ `diagnose` 仅 exit code 无 stdout 线索（B）。证据 `run_20260628203352049lf1n0l`（researcher/claude-code 崩溃）：崩前有 144 条证据事件但无最终 message → `collect` 返回 `data:[]`；进程崩时未写 stderr → `diagnose` 只给 `process exited with code 1`。codex 调度讨论文档自陈"失败 worker 的 collect 为空，验收只能读 transcript，提高整合成本"正是此债。 | **子项 A（collect 重建）**：`cli.js` collectCommand 进程分支过滤器去掉 `kind==="message"` 限制，按 runEvent.js 字段表重建所有 run.event kind（message/command/tool_use/tool_result/file_written），输出升级为事件时间线。thinking 不重建（runManager 未持久化）。`data` 元素形状从 `{info:{role},parts}`（message-only）改为 `{kind,role,parts}`（事件时间线）——契约变化但经全仓 grep 确认无调用方消费旧形状（runs.test.js:331 既有测试已同步更新断言）。**子项 B（stdout 尾留存）**：`processBackend.js` 镜像现有 stderrTail 机制加 `stdoutTail`（累积/getStdoutTail accessor/trimStderrTail 通用化为 trimTail），detail 拼装升级——stderr 优先，无 stderr 回落 stdout 尾，都无才纯 exit code。**单点修复**：不动 runManager.js（doneError 透传）/diagnosis.js（读 run.error.error 自动变富）。test/cli.test.js +3、test/processBackend.test.js +2、test/runs.test.js 同步契约。 |
| TD-79 | 2026-06-28 codex e2e | 环境隔离 | **✅ 已落地（2026-06-28，config 层 + SKILL 层双保险）**：read-only worker（researcher）跑 `pip install -e .` 污染全局 Python env——worktree 隔离了文件，没隔离 interpreter/site-packages。证据 `run_20260628203352049lf1n0l` 同一 researcher run。codex 调度讨论文档自陈"researcher 在只读任务里跑 pip install，降低对其继续承担关键路径的信任"。 | **config 层（🟢 工具域，确定性单点）**：选 layer (b) env 注入（非 layer a venv 包装——fights Windows .cmd 路径且 worker binary 非 python）。`processBackend.js` spawn env 块加 `...(agent.env ?? {})`（透传链路：effectiveAgent spread 保留 env → backend.spawn(agent) → processBackend）。`agents.example.json` researcher 加 `env:{PIP_REQUIRE_VIRTUALENV:"1",PYTHONNOUSERSITE:"1"}` 示例（OS-native：pip 在无激活 venv 时直接拒绝安装）。`cli.js` registry validator 加 env 字段形状校验（`{string:string}` 对象）。**不自动建 venv**（layer a 过重跨平台复杂）。**SKILL 层（🔵 防御纵深，归阶段 3）**：read-only worker prompt 禁 pip install 见 SKILL §1 step3。**注意**：agents.example.json 是模板，用户真实 `config/agents.json`（gitignored）需手动应用此 env——SKILL 会提示。test/processBackend.test.js +2、test/cli.test.js +2。 |
| TD-105 | 2026-07-12 coder delivery dogfood | 终态结果契约 / timeout 因果 | hard scorecard、budget、manual abort、delivery failure 的非抛出结果缺少一致 `failed`/`aborted` 布尔；wait timer 杀进程后 ProcessBackend 的非零退出可把 timeout 误报为 backend crash；真实本地驱动又发现 consumer 暂停期间 `done` 入队并 close 时，旧 EventQueue 会跳过 pending done 并误落 timed_out。 | **🟢 已偿还（2026-07-12）**：所有非抛出终态复用统一结果构造器，固定返回 completed/failed/aborted/timedOut 四布尔；wait timer 记录独立 cause，timer 触发后即使 backend 随后 `done(failed)` 也先落 `timed_out`；EventQueue 在 closed return 前重新 drain 暂停期间入队的事件。backend 自主失败仍保留原抛出契约。确定性回归覆盖 abort/hard scorecard/budget/delivery failure/timer-kill/pause+close race。 |
| TD-103 | 2026-07-10 控制面加固 Phase 2 | Coder Delivery Contract v1——控制面缺少从 isolated worktree 产出 atomic delivery commit 的能力。 | **🟢 已偿还（2026-07-13）**。Phase 2/3A/3B/3C-1/3C-2 全部 complete：`src/delivery.js`（inspect/package + plumbing + SSOT validators）；`src/deliveryVerification.js`（exact-artifact proof 纪律 + 并发 single-flight）；RunManager delivery 生命周期 + 并发原子 verification；CLI `run --delivery-spec-file`（foreground + `--isolate`）；`runs delivery --accept/--reject` + `tryAppendDecision` 原子 first-decision-wins（锁内 `_validateDeliveryFacts` 校验 exactly-one delivery + matching commit + final status）。真实 dogfood PASS（runId `run_td103_3c_dogfood_20260713`，worker coder_low / claude-code / glm-5-turbo）：terminal=completed，verification=passed，acceptance=accepted，delivery commit `81f150cc32dd8389d06d3ef7743499c6cc5c6f6b`，source checkout 未修改，loser 决策零矛盾事件，无孤儿进程、无凭据泄漏。TD-104 强隔离边界不因 TD-103 偿还而解除。 |
| TD-89 | 2026-07-08 dogfood round 5 | 能力 | `KimiCodeBackend` 与 `CodexBackend` 的 `buildArgs` 完全不引用 `agent.systemPrompt`——`config/agents.json` 里 6 个 agent 全声明了 `systemPrompt`（指向 `config/roles/*.md` 角色契约），但只有 claude-code 的 4 个真正生效。`coder_mm`（kimi-code）和 `tester`（codex）的角色契约 prompt 文件写好了却没注入 worker——这两角色跑起来拿不到身份/边界/纪律引导。**配置静默失效**：字段写了、`registry validate` 不报错、但被忽略。 | **🟢 已偿还（2026-07-22，M11-5 Package A + A2 + B + C）**：共享、backend-neutral 的角色合同加载器 `src/application/roleContract.js`（fail-closed：缺失/目录/空/超 4096 字节/非法 UTF-8/NUL/unsafe 控制字符均在副作用前拒绝，错误是固定安全形状）。角色注入决策由 backend 能力声明严格驱动（`supportsRoleContract === true`，非 truthy）；不支持注入的 backend 配 `systemPrompt` 在 start（创建 transcript 前）/ resume（读取既有 transcript 后、append/spawn 前）fail-closed。**Package C 跨项目 + 严格性收口**：相对 `systemPrompt` 由 `resolveRoleContractPath` 相对 WAO 安装根解析（非 cwd），同一份全局 registry + 角色文件可从任意目标项目目录使用（已从 Life Index cwd 验证 `registry validate` 全过）；registry 用 own-property 语义（`systemPrompt` 属性存在但 undefined/null/空白/非字符串均拒绝，属性缺失才表示无角色）。三个 process backend 各自注入：claude-code `--append-system-prompt <内容>`（恰好一次，用内容不用路径消除 TOCTOU）；codex `-c developer_instructions`（不替换 base instructions，禁止 model_instructions_file）；kimi-code 固定分隔组合 role+task（非系统级权限隔离）。Lead/model 不能覆盖角色（run_dispatch strict schema）。**WAO 不把角色合同保存为 `prompt.sent`/控制面输入**（transcript 只持久化原始 task prompt）；注意 worker 输出可能在回答中引用或复述角色，故不宣称 transcript 永远不含角色正文。Package B 三条真实 canary 全通过（runId 锚点见 roadmap；详细证据留 gitignored `.dev/`）。机制细节见 `docs/02-architecture.md`。 |
| TD-131 | 2026-08-19 lane 架构前置批次 0（认证门复核，Owner 批准；登记+收口同轮） | 认证门身份洞 | P1-1 认证门（CLI 显式 `--require-certified`）从不比对 summary 记录身份与当前 registry 配置——改 `config/agents.json` 换 backend/model 后不重跑 reliability，窗口内**旧组合**的认证照常放行。显示层 `matchedCertRecord`（registryInventory.js）早有 backend+modelId 比对（docs/troubleshooting.md §7.12 描述的是该显示路径），门没读，两处判定分叉。 | **2026-08-19 已偿还（同轮收口）**：`matchedCertRecord` 抽为单一 SSOT，host 在 `src/runManager.js`（core；registryInventory.js 原 private 副本删除、改下向 import——application→core 合法边，与 runDispatch.js 消费 R10-A/R11-1 校验器同一先例；不能反向：独立 application 模块对 core 是上向边，TD-122 空白名单）。门在 cleared 旁路之后、status 之前做身份比对：backend/modelId 记录侧声明即比对（agent 侧 null 归一）；**providerID 为新增比对维度但仅双侧声明时生效**（claude-code 形状无 `model.providerID` 的 agent 仍由 backend+modelId 把关；不新增 agent registry schema 字段）。不匹配按未认证拒绝，固定文案不回显 summary/registry 值（fixed-safe 先例）。显示层同步收口：`buildCertMap` 此前**丢弃** `record.providerID`，已补透传。行为变更条目见 docs/usage.md（run_dispatch 节）。**验收会审补登已知边界（2026-08-19 深夜，auditor F3 + coder_mm b3）**：(i) provider 块（`provider.baseUrl`/`apiKeyEnv`）是**完全未比对的身份维度**——5 个进程式角色 lane 全用 provider 块表达 provider（无 `model.providerID`），改 baseUrl 指向另一家端点而 model.id 不变，旧认证照常放行（T4b 钉住的既定行为；同族面另见 runContinue.js:410-413 续跑漂移检查）；根修需按 ADR 给 provider 块正式 id 维度。(ii) legacy 容忍只覆盖字段 undefined，不覆盖显式 null——带 `backend:null` 的旧 summary 判不匹配（方向 fail-closed 可接受，T4d 未钉此形状，触碰时补）。 |
| TD-132 | 2026-08-19 lane 架构前置批次 0（认证门复核，Owner 批准；登记+收口同轮） | 认证门新鲜度洞 | P1-1 门的新鲜度读整份 summary 的全局 `generatedAt`——重考**任一** worker 即刷新全本台账，其余 worker 的陈旧认证被"洗白"（per-worker 的 `lastHealthyRunAt` 已由 `scripts/reliability/certification.mjs` 写入 summary，门没读）；且 `generatedAt` 缺失/不可解析时旧逻辑（`Number.isFinite` 短路）竟放行。 | **2026-08-19 已偿还（同轮收口）**：门改读 `w.lastHealthyRunAt`（per-worker 新鲜度）——缺失/null/非字符串/不可解析一律 **fail-closed** 拒绝（固定 reason）；过期 reason 只回显计算出的天数（不再回显磁盘上的原始时间戳文本）。`manualOverride:"cleared"` Owner 背书旁路行为不变（仍先于 status/身份/新鲜度放行）。行为变更条目见 docs/usage.md（run_dispatch 节）。 |
| TD-136 | 2026-08-20 R23-A 库卫生（TD-131 行内注记升格） | 【认证门身份维度】 provider 块（baseUrl/apiKeyEnv）完全未比对——3 条 lane 用 provider 块表达接入方，改 baseUrl 而 model 名不变旧认证照常放行。 | **2026-08-21 已偿还（R23-C，ADR-0026）**：providerKey = baseUrl 指纹+apiKeyEnv 变量名（fingerprint-only，无 provider.id——可选 id 是旁路已否决）；单一实现 src/providerFingerprint.js（CORE_TOP 登记）；四写入点接线（matrix 从 registry 派生禁行硬编码）；台账合并边界钉死（prior-only worker 字段保持缺失，半迁移不连坐）；run.started 持久化 providerKey（无条件键）+ runContinue 漂移比对。实施=Owner 双执行席对比实验（coder_ox 基线 6c36753 + coder_hq §4/测试吸收轮）；不宣称全闭合：共享网关拓扑无判别力（该面判别器 apiKeyEnv+modelId，现状不变）。 |
| TD-113 | 2026-08-14 Lead dogfood | 【可观测粒度】 长时单文件写作场景 `lastActivitySummary` 停留同一文件名约 20 分钟（coder_hq 重写 02-architecture.md 实证），无法区分"在推进"与"卡死"。TD-75/76 已做心跳，缺"长文档/长代码写入"类长操作的节拍信号。 | **2026-08-20 已偿还（R23-B，coder_hq run_20260820194805967evsdah / commit 01826943 + Lead 补全）**：`describeActivity` 可选参演进（缺省逐字节不变，TD-113-DEFAULT 钉）+ `countRecentSamePathWrites`（file_written 子序列尾部同名计数，真实交错形状有效上限约 20-30 次写——检视上界 200 条事件；计数纯位置式无时钟）；数据源=调用点绑定 scope（外 run 尾条不灌入，R23-B-1 探针）；CLI `runs status` 摘要显示 `写 <file> ×N（最近）`。同轮姊妹项 round4 F-2（长静默判读）以 troubleshooting §6.8 关单（双维度：计数=推进、secondsSinceActivity=时间，CLI/MCP 字段名并列）。**CLI-only**：MCP `run_status` 有意不返回 lastActivitySummary（安全子集，负断言钉死）——MCP 面同能力见 TD-139。 
| TD-4 | M0 | 【架构】 stopCommand 直调 backend.abort 不走 RunManager（不写 state_change）。语义：stop 操作历史 run 可能已不在 activeRuns | **2026-08-20 销账（R23-A 库卫生，按符号验证）**：`src/application/runStop.js:226/:335` stop 路径已统一走 `transitionState` 写 `run.state_change`（first-terminal-wins，TD-99/TD-100 契约族；runStop.js:7 头注释自陈）——登记的处理条件"M7 resume/恢复统一两条路径（active→RunManager.abort，历史→transcript 追加）"描述的正是现行实现。 |
| TD-51 | 2026-06-26 TD-37 收口 | 【测试基础设施】 **✅ 已解（2026-06-26，TD-37 收口同轮）**：原问题——reliability 的 `runCli` 用 `execFileSync` spawn CLI，CLI 内部 detached background runner（`spawn` 路径）被回收，stop drill 拿不到 transcript。**真实根因有二**（收口时挖出）：① `execFileSync` 在 Windows 退出时清理进程树连杀 detached 孙进程 → 改用 `spawnSync`（直接 spawn不清理进程树）；② **detached runner 继承 CLI 的 cwd**——cwd=TMP_DIR 时 runner 找不到 registry/config 秒退 → stop drill 的 spawn/stop 改用 `cwd=ROOT`（项目根）。两修后 stop drill 端到端通过（见 TD-37 认证）。顺带修了 runCli 走 v22 shim + runDir 错位 + waitForTranscript + 预创建目录。 | ✅ 已解；stop drill 端到端认证通过（TD-37 清零） |
| TD-90 | 2026-07-08 dogfood round 7 | 【环境注入】 注入给 worker 子进程的 `$WAO_CLI` env（`processBackend.js:113`）原指向裸 `src/cli.js`——三处注入点（`cli.js:2219`/`backgroundRunner.js:97`/`daemon.js:313`）都是 `join(dirname(fileURLToPath(import.meta.url)), "cli.js")`。worker 在自己 shell 里调 `$WAO_CLI wao handoff write` 时，shell 默认 `node` 常是 v24，直接执行触发 `nodeVersionGuard` 被拒。Lead 侧走 `npm run cli`（package.json script 经 `scripts/wao-node.cjs` v22 shim）无此问题，但 worker 侧没有这层包装——**Lead 与 worker 的 WAO 入口不一致**。dogfood round 7 实证：coder_hq worker 踩此坑，自行绕路用 v22 node 绝对路径修正才写入 handoff（浪费一次 token + 一次无效命令）。详见 `.dev/friction-log/dogfood-r7-worker-wao-cli-node24.md`。 | **🟢 已修复（2026-07-08）**：新增 `src/waoCliPath.js`（SSOT helper），`getWaoCliPath()` 在 win32 返回 `scripts/wao-cli.cmd`（内部用 v22 node 绝对路径 `%LOCALAPPDATA%\Programs\nodejs-v22\node.exe`，不经 PATH，定位逻辑同 `wao-node.cjs`）；非 win32 回退裸 `cli.js`。三处注入点改为引用 helper（防漂移）。`scripts/wao-cli.cmd` 纯 ASCII 注释（曾因中文注释导致 cmd.exe REM 行解析错位）。test/cli.test.js 加 TD-90 测试（win32 验证返回 .cmd 且文件存在）。 |
| TD-91 | 2026-07-08 dogfood round 5/6 | 【结构缺陷】 STAGE-/DECL- 正文文件 + 索引行写进 `.wao/decisions/`（`waoStage.js:67/97` + `waoDeclare.js:56/82` 用 `appendMapIndex` 往 `decisions/map.md` append），而 `decisions/` 被 git 跟踪（gitignore `!.wao/decisions/` 例外，本意让 ADR `NNNN-*.md` 随代码版本化）。**STAGE/DECL 是运行时声明（每轮临时），ADR 是冻结决策（随代码）——两者性质不同却进了同一 git 跟踪目录**。后果：round 6 的 6 个 STAGE 正文 + map.md 索引行混入主仓库被 git 跟踪，含本机绝对路径触发脱敏门控；round 5 也踩（当时误报，但机制同）。TD-84 的 `--cwd` 纪律防不住——Lead 用 `--cwd <WAO根>` 时 STAGE 理所当然写进主仓库，map.md 是 append 式索引，实验声明一写就和真实 ADR 混在一起。 | **🟢 结构修复（2026-07-08）**：新增 `.wao/pipeline/` 第 6 槽位（运行时声明专用，gitignore）。STAGE-/DECL- 正文 + `pipeline/map.md` 索引从 `decisions/` 挪到 `pipeline/`。`decisions/` 只留 ADR（`NNNN-*.md` + decisions/map.md 只含 ADR 索引）。改动：`waoDir.js`（`WAO_TOP_LEVEL_SLOTS`/`SLOTS_WITH_MAP`/`MAP_HEADER` 加 pipeline）、`waoStage.js`+`waoDeclare.js`（路径 decisions→pipeline）、`cli.js`（init slots + dashboard 读路径）、`waoLayout.test.js`（守卫 5→6 槽位 + STAGE/DECL 不在 decisions）、gitignore 注释。 |
| TD-94 | 2026-07-08 真实任务复盘 #2 | 【gitignore 契约】 `ensureGitignore()`（`waoDir.js:154-159`）写裸 `.wao/`（全忽略），导致目标项目 `decisions/` ADR 永远不入库，违背 SKILL "decisions/ 随代码版本化"契约。WAO repo 自己的 .gitignore 是手工修正的正确三行规则（`.wao/*` + `!.wao/decisions/`），但从未传进 ensureGitignore。 | **🟢 已修复（2026-07-08）**：ensureGitignore 改写三行规则 + 幂等检查改为检测 `!.wao/decisions/`（精确规则）。test/waoDir.test.js 更新断言。 |
| TD-95 | 2026-07-08 真实任务复盘 #1/#4/#5/#6/#7/#8/#11 | 【信任 + 可观测性 + 跨项目】 真实任务复盘 12 项发现中 7 项可修（排 #9/#12 正面 + #3 已在 TD-93）。批量修复按 TDD plan B1-B5 推进：(B1 #2) gitignore 契约→TD-94；(B2 #5) 状态悖论——`runManager.js` failed 路径不查证据直接判 failed，加 `_auditEvidenceOnFailure()` 写 `run.evidence_audit {passed:true}`；(B2 #6) error 截断到 500 字符 + 注入 diagnosis；(B3 #7) stage artifact run 路径解析为绝对；(B4 #4) diagnosis 加 `no_effect` category；(B4 #5) diagnosis 加 `evidence_passed_backend_failed` category；(B4 #1) doctor 多余目录给迁移建议；(B4 #11) doctor `--strict` 跑 parse smoke。详见各 commit。 | **🟢 全部已修复（2026-07-08，B1-B5 五批提交）**。核心是 #5 状态悖论——worker 做对了但 backend 崩了时，终态仍 failed（不撒谎）但 evidence audit 让 Lead 知道"证据其实通过了"。 |
| TD-98 | 2026-07-09 外部审计 | 【架构债务（cli.js 过胖）】 `src/cli.js` 约 2407 行，同时包含 CLI 路由 + doctor parse smoke + doctor 健康诊断 + stage artifact 解析 + runAndWait 失败结构化 + resolveArtifactPath + _doctorParseSmoke。已偏离架构文档"CLI 入口与路由"的边界（02-architecture.md:548）。审计指出在继续做 TD-96 stage auto 前，应先拆 `src/commands/doctor.js`、`stage.js`、`runResult.js` 等命令模块。 | **🟢 已偿还（2026-07-09，TD-98 stage 1→2e-3 全部完成）**。cli.js 从 2407 行降到 **348 行**，最终只承担配置（loadConfig/getConfig）、路由（main）、help（printHelp）、DI/入口（waoCommand wrapper 注入 askHandler；waoAskCommand 留 cli.js 调 commands/run.js 导出的 runCommand）职责。已拆出的 commands 模块：`daemon.js`、`registry.js`、`runs.js`（buildDashboard/runsDashboardCommand）、`workflow.js`、`worktree.js`、`wao.js`（waoCommand + resolveArtifactPath）、`doctor.js`（waoDoctorCommand + parse smoke + CLI/oauth 检测 helper）、`observe.js`（status/tail/collect）、`stop.js`（stopCommand + killProcessTree）、`lifecycle.js`（retry/resume）、`run.js`（run/spawn/runAndWait + scorecard helper + spawnBackgroundRunner，持有 runCommand/runAndWait）、`shared.js`（parseOptions/loadPrompt/resolveTargetCwd/resolveIsolateFlag/newRunManager/backendFor/loadRun）。**依赖方向**：cli.js → commands/*.js；commands 依赖 shared.js 及 transcript/backends 等底层模块；commands/*.js 不反向 import cli.js。test/cli.test.js 的 9 个 import 符号全部由 cli.js re-export 保持契约不变。**路径修正**（从 src/ 搬到 src/commands/ 后多一层 `..`）：workflow.js 的 templates 目录路径、doctor.js 的 parse-smoke 源码目录路径、run.js 的 backgroundRunner.js 路径 + runAndWait 动态 import 的 diagnosis.js/transcript.js 路径。 |
| TD-99 | 2026-07-09 测试 gate 稳定化 | 【并发时序 race（waitForCompletion 外部 abort）】 `Run.waitForCompletion` 处理 `done(failed)` 事件后，靠 `_externalTerminalState()` 读 transcript 防御"外部已写 aborted 终态被覆盖"。但存在窄 race 窗口：当 `done(failed)` 到达（runManager.js:558 break）与 `_externalTerminalState()` 读取之间，若外部 append 的 `aborted` 尚未落盘（并发 `appendFile` + seq lock I/O 延迟），`_externalTerminalState()` 返回 null，落入 `doneReason==="failed"` 路径写 `failed` 终态 → 覆盖了 aborted。test/runManager.test.js TD-55 单跑稳定（3/3），仅在高并发 npm test 负载下偶发。**注意**：外部 abort 在生产里只来自 `stop` 命令或 daemon，不是常见路径；且 `_aborted` 内存 flag（_abortInternal 设）覆盖了内部 abort 路径——本 race 仅影响"外部进程直接写 transcript"的边缘。 | **🟢 已偿还（2026-07-10）**：方案 B（原子终态 claim / first-terminal-wins）。`JsonlTranscript.transitionState` 在已有 append lock 内一次完成"读事件→检查既有终态→分配 seq→批量 append"（单次 appendFile），rejected 写 `run.state_change_rejected`（attemptedTo/attemptedReason/existingTerminal/reason）。所有 writer 接入（rg `append("run.state_change"` src/ 零命中）：RunManager._transition、stop.js、backgroundRunner、run.js。**terminal fact 原子提交**：run.completed/run.timed_out/run.aborted 通过 factEvents 与 state_change 同批；rejected 不留 fact。**start rejection**：pending rejected 不 spawn；submitted rejected abort handle+cleanup+不注册。**diagnosis legacy 兼容**：旧双终态 transcript（aborted 后 race 写 failed，>=2 terminal state_change + 第一条 aborted + 无 rejected）按 legacy 归 aborted_manual；新世界 failed 先赢归非 aborted，aborted 先赢归 aborted_manual。findState 仍 last-wins（不回写历史）。**测试**（npm test 714/714）：test/terminalArbitration.test.js 14 个（13 同进程确定性 + 1 真实跨进程 fork+IPC barrier，均不依赖 sleep）+ runManager.test.js 3 个（start rejection: terminal transcript spawn=0；submitted rejected 用 spawnEntered barrier 无 setTimeout）。 |
| TD-100 | 2026-07-10 dogfood 发现 | 【进程安全 / 控制面可信度（stop 副作用先于终态所有权）】 `stopCommand` 旧顺序是"先 taskkill/backend.abort，再 transitionState claim aborted"——rejected loser 仍执行破坏性副作用（taskkill/abort）。dogfood 实证：两个并发 stop 都报告 taskkillCalled=true，但 transcript 只有一个 terminal state_change（loser 也杀了进程）。`killProcessTree` 不检查 taskkill exit status 却返回 verified=true（假阳性）。`run.stop_requested` 缺 reason 字段 → diagnosis 显示 reason=unknown。已 completed 的旧 run 仍被 taskkill（PID 可能复用 → 误杀风险）。 | **🟢 已偿还（2026-07-10）**：方案 B（先 claim 再 kill）+ 审计收尾（attemptEvents/EPERM/有界轮询/invalid_pid）。**claim→kill 顺序**：stop.js 先 transitionState claim aborted{verification:"pending"}，只有 accepted winner 才执行 kill/abort。rejected loser 零副作用。**attemptEvents（审计 P1 修复）**：stop_requested 不再 claim 前单独 append（旧实现产生 SSOT 分叉：findState 把 legacy stop_requested 读成 aborted，但 _detectExistingTerminal 排除它 → 读取器和仲裁器结论不一致）。改为通过 transitionState 的 attemptEvents 同批提交——持锁读取的旧 events 不含本次 attemptEvents，不自拒绝。_detectExistingTerminal 恢复与 findState 同源的 legacy fallback（不再排除 stop_requested）。**PID 存活判定（审计 P1/P2 修复）**：isPidAlive 仅在 ESRCH 时返回 false；EPERM/未知错误保守 true（不得假验证）。isPidAlive 探测函数可注入（isPidAlive(pid, probe=process.kill)），测试用注入 probe 模拟 ESRCH/EPERM/未知错误，不依赖真实 PID 环境。taskkill 后有界轮询 waitForPidExit（默认 5×200ms，可注入），补偿 Windows PID 回收延迟。轮询耗尽仍 alive → unverified+raiseAlert。**无效 PID 边界（最终修复）**：stop 命令在 claim 前验证 process session PID（Number.isInteger && >0）。无效 PID（proc_not-a-number/proc_0/proc_-1）不进入 processStop，不 claim 终态，不 taskkill，保持 run 原状态。记录 stop_requested + stop_unverified{outcome:"invalid_pid"} + raiseAlert。把"metadata 损坏"与"进程已退出"区分开（前者 outcome=invalid_pid/stopped=false，后者 outcome=already_exited/verified=true）。**stop_verified 证据完整性**：run.stop_verified/unverified 均写 outcome/taskkillCalled/taskkillExitCode/processAliveBefore/processAliveAfter（进程路径）或 backend/method/taskkillCalled（opencode 路径）。CLI JSON：stopped=verified（rejected/unverified/invalid_pid=false）。stopCommand 加 deps 参数（kill/isAlive/waitForExit/sleep/pollConfig/executeStop/alert 注入）。test/stopSideEffect.test.js 26 个 + test/terminalArbitration.test.js 新增 3 个（attemptEvents + SSOT 统一），全依赖注入，不用真实 PID/sleep。npm test 全绿。 |
| TD-101 | 2026-07-10 控制面加固 Phase 1 | 【角色合同泄漏编排（worker system prompt 含 WAO 落盘指令）】 `config/roles/*.md` 在 worker 角色 system prompt 中包含编排落盘指令（`$WAO_CLI wao handoff/decision`、`$WAO_TARGET_CWD`），使 worker 看到编排层命令——违背"worker 只看任务 prompt，编排逻辑不下沉"原则。Chief-Auditor 角色卡未明确为 Lead Agent 的平级合作伙伴，且含"求完整上下文"（无界）。 | **🟢 已偿还（2026-07-10，Phase 1 Batch 1A）**：6 个 role file（researcher/coder_hq/coder_low/coder_mm/tester/auditor）删除所有 `$WAO_CLI`/`WAO_TARGET_CWD`/`wao handoff`/`wao decision` 编排标记，替换为"在最终 assistant response 中交付结果"指引。auditor.md 重写：明确 Lead Agent 平级合作伙伴身份、上下文与风险相称（非无界）、不承担实现和最终拍板、可提可验证替代方向、未授权不改文件。docs/team-roles.md 同步核心原则和角色矩阵。test/roleContracts.test.js 5 个测试守卫（编排标记零容忍 + 章节完整性 + auditor 合同精确断言）。npm test 全绿。 |
| TD-102 | 2026-07-10 控制面加固 Phase 1 | 【workflow outcome 语义分叉（findState/.wao state/runs 聚合/workflow CLI 互相矛盾）】 4 个观测面报告不同结果：(1) `findState` 把 `workflow.completed{completed:false}` 映射为 `completed`（只看 type 不看 payload）；(2) `writeStateSnapshot` 只按 executed 数量判断终态，skipped 节点不算 resolved → 失败传播后永远 `in_progress`；(3) `runs list/summary/metrics/dashboard` 用 `endsWith(".jsonl")` 扫描，把 `wf_*` workflow transcript 当 worker run 计入；(4) `WorkflowEngine.execute()` 不返回 skipped 节点，CLI 漏掉被跳过的节点。审计收尾发现额外缺口：(5) 超时 break 跳过层末快照写入 → state 停在 in_progress（应为 failed）；(6) timeout 未执行节点既不在 nodeResults 也不在 skipped → CLI 遗漏；(7) router skip notes 错误写 "upstream failed"。 | **🟢 已偿还（2026-07-10，Phase 1 Batch 1B + 审计收尾）**：**findState**：legacy fallback 读 `workflow.completed` payload——`completed:false → failed`。不改 run 终态仲裁 / TD-99 / TD-100。**writeStateSnapshot**：resolved = executed ∪ skipped；全 resolved + anyFailed(executed only) → failed；全 resolved + 无 failed → completed；新增 `finalStatus` override（engine 超时/失败时强制 failed）。**runs 聚合**：新增 `loadRunOnlyFiles`（只 `run_*`），list/summary/metrics --summary/dashboard/forecast 改用此函数；grep/prune 保留 `loadRunFiles`（所有 .jsonl）。**workflow result**：`execute()` 新增 `skipped` 数组返回。**timeout 最终快照**：for 循环结束后 engine 写一次最终 state snapshot（`!overallCompleted → finalStatus:"failed"`），超时节点保持 pending/not_executed 但 workflow 总状态 failed。**CLI 全节点渲染**：`workflowRunCommand` 按 `effectiveDef.nodes` 遍历——有结果 `{completed,runId}`，skipped `{completed:false,skipped:true}`，未执行 `{completed:false,skipped:false,notExecuted:true}`。**router skip notes**：waoState 改中性 notes "skipped"（不猜测 upstream_failed vs route_not_selected）。direct `status wf_*` 仍可用且报正确终态。test/transcript.test.js +2、test/waoState.test.js +2、test/runs.test.js +5、test/workflow/engine.test.js +1、test/workflow/engineStateIntegration.test.js +3（timeout state=failed + timeout 不伪装 skipped + router notes 中性）。npm test 全绿（两轮）。 |
| TD-126 | 2026-08-18 R12-C auditor 窄复核（run_20260818094046993bk0doq） | cliHelp `run --help` 正文两处 "only on run (not spawn/workflow/daemon)" 措辞自 R12 起过时（retry 面已接受 --model/--reasoning）；HELP_TEXT 与 docs/surface/cli.md 均已正确，仅正文两句漂移。 | **已修复（commit b06c2c7，2026-08-18 R12-C 交付；R23-A 按符号验证移入）**：`src/cliHelp.js:134/:149` 正文已是新措辞（only on run and retry (not spawn/workflow/daemon)）；钉住新措辞的两条守卫同步在库——`test/isolation-infra/cli.test.js:3773/:3775` 与 `test/isolation-infra/docs-consistency.test.js:2982/:2984`（含旧句反回归）；纯销账零实现。原注保留：守卫是防回潮锚不是设计意图，别误读。 |
| TD-127 | 2026-08-18 R12-C auditor 窄复核（NEW-FINDING，先于 R12 既存） | retry 的任务文本取法是无绑定 `findLatest(events,"prompt.sent")`（lifecycle.js:45）——与 R12-C 修掉的 run.started 侧信道同一 append-only 原语且威力更大（改整段任务指令；探针实证同 runId 与跨 runId 伪造都会被派发）。同一无绑定取法还喂 runCollect.js:242 与 resume 链。 | **🟢 主体已修（R13 + R13-C）**：retry 面 R13 起绑定；R13-C 删 messageId 优先收窄、回归纯 `findLatestBound`（收窄对 ProcessBackend 家族是死代码——合法双写落盘均无 messageId），D 形状测试钉当前行为（cli.test.js R13-CLI-3），夹具诚实化（假 claude-code backend 不再返回 messageId）。R13-C 同修 resume 链（session.created/run.started 首条绑定、replay prompt 与 attach messageId/admittedSeq 末条绑定；legacy 无信封 → null 拒绝，Lead 接受）与 stop 杀进程 lane（runStop.js session.created/run.started 末条绑定——修复前尾部外 run 伪造 proc 可夺走 kill 目标）。runCollect lane（:184/:242）原维持 R13 的"不改"决策（Owner 级）——**已于 R16（本次交付，2026-08-18）修复**：Owner 拍板不兼容信封前老数据（本机实测存量 pre-envelope transcript 为 0）后放行；`:184` session.created → `findLatestBound`（末条序，与 stop/activity 同族——serveUrl/sessionId 整体重定向面）、`:242` run.started → `findFirstBound`（首条纪律，R12-C/R14-C 先例——serve fetch directory 参数重定向面；漏网清点：文件内 findLatest 恰两处、无 events.find）。旧格式 transcript 降级为既有"无会话元数据"拒绝（零 fetch 零 append，usage.md run_collect 行为变更条已列）；探针 R16-CO-1/2/4/5 经变异自证红（R16-CO-3 为全绑定序语义钉，设计上不红）。run_202608181759397651p0g15 / on-main commit 5131bfb（验证两遭 harness 环境性失败后按契约 reject+前作集成，TD-130）。**残余（自明）**：本条偿还范围 = retry/resume/stop/runCollect 四条绑定 lane；同类读绑定残留面不在本条偿还范围，整体维持登记于 TD-128（开放区维持清单）。 |

---

## 开放（🟡）

| # | 登记于 | 类别 | 内容 | 触发条件 / 何时处理 |
|---|--------|------|------|---------------------|
| TD-108 | 2026-08-13 post-M12 独立审查 | 诊断事实绑定 | legacy evidence fallback 调用 `assessRunEvidence(events)` 时没有按 `expectedRunId` 过滤；当前生产读取路径是一份 transcript 对应一个 runId，独立审查抽样 120 份 transcript 也未发现混合 runId，因此没有当前误分类事实。 | 保持开放观察，不在本包扩大代码范围。若未来需要接受混合/损坏历史 transcript、共享事件数组，或出现跨 run evidence 污染实证，则在共享 assessment/caller 边界按 expected runId fail-closed，并补真实 transcript 回归；不得仅凭理论可能改变当前单-run读取语义。 |
| TD-104 | 2026-07-12 coder delivery dogfood | 凭据边界 / 输出脱敏 | ProcessBackend 曾把完整 `process.env` 交给 worker；worker 工具可枚举无关 provider keys，原始/解析输出也可把已知 secret 写入 raw capture、内存结果、CLI 与 transcript。 | **紧急暴露面已收敛，强隔离债务保留**：进程 worker 改为 OS/runtime allowlist + backend-assigned credential channel；secret-like `agent.env` fail-closed；wrapper 映射后按大小写不敏感规则删除 source alias；显式 credential channel 与 credential-like env value（最短 8 字符，含 proxy）进入同一 exact-value redactor，覆盖 RunManager memory、transcript append/transition/reason、stdout/stderr tail 与 UTF-8 跨 chunk raw capture；payload 不能覆盖 transcript envelope。Owner 已轮换受影响凭据。**已知边界**：同一 OS identity 下 worker 仍可读取 assigned runtime token/credential files；OpenCode shared server 不在 ProcessBackend 边界内。按 decision 0016，监督式 Phase 3C 可在单一 process-backed coder、单一可撤销凭据和 Lead 验收下继续；credential broker 或独立身份仍是无人值守、多租户或强隔离发布前置条件。事故见 `docs/incidents/2026-07-12-worker-credential-exposure.md`。 |
| TD-13 | M2 | 跨平台 | ProcessBackend abort 用 Windows taskkill /T /F | 需跨平台时加 platform 分支（AGENTS.md 约束只 Windows） |
| TD-15 | M2 | 语义 | ProcessBackend retries 参数被忽略（进程式不幂等重试） | 需进程级重试（如 OOM 重启）时定义语义 |
| TD-23 | M3 | 未接入 | portAllocator 实现了但 RunManager 没用 | M5+ DAG 并行多 opencode-serve 实例时接入 |
| TD-24 | M4 | 精度 | opencode token 是"最后一条 assistant 累计值"非增量，多轮可能偏高。**部分覆盖**：metrics 已改用 session endpoint 累计值（P0-B 修复），比 message.info.tokens 准。但 session 级仍是累计非分轮，需精确分轮时再处理 | 需精确分轮 token 时处理 |
| TD-29 | M5 | 内存 | 大 DAG（100+ 节点）completedResults Map 内存占用 | 实际遇到大 DAG 时做流式/分页 |
| TD-31 | M5 | 安全 | workflow .mjs 有完全 JS 能力（和 npm script 同级风险） | 文档标注即可（与 npm script 同级，非新增风险） |
| TD-34 | 文档审计 | 维护 | `test/docs-consistency.test.js` 的断言是文档内容指纹（关键词/端口/编号）。文档大幅重构时需同步更新断言（断言≠文档质量本身，只是不变量守卫） | 文档结构性重构时同步断言 |
| TD-41 | 2026-06-23 测试稳定性 | 偶发 | `test/processBackend.test.js` 的 `abort 能杀掉长时进程` 在全量 suite 高负载下曾偶发失败：`taskkill /T /F` 后 Node 回收子进程 exitCode 可能延迟；隔离运行稳定。 | 保持开放观察；当前 canonical 由 TD-107 的资源波控制并发。若再次稳定复现，优先用 child `exit` 事件建立确定性因果，不以扩大 timeout 掩盖。 |
| TD-45 | 2026-06-25 M7-P3-T1 | 可靠性 | **✅ 已落地（2026-06-26 修正登记）**：daemon 自愈已实现为 `src/daemonSupervisor.js`——独立 detached supervisor 进程（CLI 一次性 spawn），轮询 daemon 心跳，判死→重启 daemon（带 `--resume-on-start` 接管未完成 run）+ 退避防风暴（`decideSupervisorAction` 纯函数：风暴退避 > 判死重启 > 空闲自退 > noop）。**残留边界**：supervisor 自身被杀（如机器重启）无法自拉——"重生引导"需 Windows 服务/计划任务，留 v2。 | ✅ 主体已落地；supervisor 自身崩溃后的重生引导（Windows 服务/计划任务）留 v2 |
| TD-46 | 2026-06-25 M7-P3-T1 | 可靠性 | **🟡 可观测已就位，根因待真需求**：daemon 进程组累积清理 + 句柄/内存泄漏。daemon 常驻数小时/天会累积 worker 子进程残留、句柄泄漏——单测覆盖不到。P5-T2 已加监控（assessDaemonHealth，rss/heap/worktree/activeRuns + 阈值告警）；P5-T3 长跑 dogfood（45min/265run）**本轮未触发**（保守阈值下）。 | 🟡 根因修复留待真需求：更长时长/更高负载触发 health warn 后针对性修。监控 ✅ 已落地（T2） |
| TD-47 | 2026-06-25 M7-P3-T1 | 能力 | **🟡 划给 P5/v2**：tail 流式 IPC。P3-T1 daemon IPC 只做请求-响应（ping/list/status/start/stop/shutdown），tail 沿用文件轮询（现有 `tail --follow`）。流式推送需 IPC 长连接 + 多路复用/分帧，与"窄协议换简单"取舍相悖，故推迟。 | 🟡 真实需求驱动时再加（SSE 式推送 over 命名管道，或保留文件轮询） |
| TD-81 | 2026-07-02 friction log 核实 | 可维护性 | TD 表"已偿还"声明 vs 代码事实之间缺机器守卫。元发现：核实 06-28 friction log 的 16 条收口时，靠人工 `rg` 逐条核对 8 条"已偿还"TD 均在代码中有行号级证据（属实），但若 TD 表被误改（标 ✅ 但代码回退了）**无测试会红**。根因：本仓是 snapshot（原始 commit 在私有仓库，无 hash 可溯），某条 TD 标 ✅ 但偿还信息空/残缺时无法自动发现。 | **🟢 工具域（部分）已落地（2026-07-02）**：`test/docs-consistency.test.js` 加守卫——扫描"## 已偿还"区每条 TD 行，断言"偿还于"列非空且含可识别偿还标记（里程碑 M\d / 日期 2026- / 偿还语）。守住"偿还声明自身一致性"底线。**未覆盖**："代码事实校验"维度（对每条 ✅ TD 断言其声称修改的源文件/符号仍存在）需手写源文件映射，成本高且 TD 描述非结构化，留待真实漂移被发现后再做。证据素材见 `.dev/friction-log/2026-06-28T17-00_*.md` 二次核实表 |
| TD-82 | 2026-07-02 编排质量 | 编排行为 | Lead 倾向"自己包揽"而非派工——三次独立观察（06-28 WAO core log + 06-28 GUI log + 07-02 用户观察）浮现同一现象。根因：SKILL 派工章节有三个结构性缺陷——(a) 语气默认值反了（"自己做"列第一选项、处处"无硬红线可偏离"）；(b) 成本账本不对称（只算派工成本，自己做的隐性成本——Lead 用最贵模型干 leaf 活、串行、上下文污染、无 transcript——没进账）；(c) 摩擦方向反了（自做零摩擦、派发高摩擦）。TD-73 曾把"自做写一句理由"作软建议，但实证软建议不起作用。 | **🟢 文档 + 控制平面双层落地（2026-07-02）**：**文档**（SKILL B1-B5）——角色定位加"默认派发"声明；"何时自己做 vs 派发"反转为派发优先 + 对称成本账本；"自做可派任务写一句理由"升级为"必须 `wao declare`"硬声明。**控制平面**——新增 `src/waoDeclare.js`（reason 枚举 SSOT + DECL- 前缀文件 + map 索引）+ CLI `wao declare --task --reason <code>`（枚举校验防自由文本逃避）+ `runs dashboard` 汇总 selfDeclared 维度（曝光机制）。强制力 = 曝光（可见）不是拦截——WAO 看不见 Lead 非 WAO 工具调用，只能靠主动声明。**验证靠真实 dogfood**：Lead 派工率是否上升 + declare 使用模式是否合理。test/waoDeclare.test.js +6、test/cli.test.js +3、test/docs-consistency.test.js +1（理由码枚举一致性守卫） |
| TD-83 | 2026-07-07 编排质量 | 编排行为 | TD-82 落地 declare 后，dogfood（07-07 用户观察）发现 Lead 的"敷衍"模式仍存——只派一次 researcher 然后剩下全自己干完。根因比 TD-82 更深一层：SKILL 的"职责链"是**散文建议**（理解→编排→派发→验收→整合→汇报），阶段 1/2/5/6 没有任何产物 gate，Lead 能跳过任意一步且跳过 = 隐形。declare 管"派工 vs 自做"，但管不了"走了 pipeline 哪几步"——这是 declare 没覆盖的正交维度。 | **🟢 文档 + 控制平面双层落地（2026-07-07）**：**文档**（SKILL）——把散文职责链重写为"6 阶段产物门控 pipeline"（理解→编排→派发→验收→汇总→总结），每阶段标明**产物** + **门控声明**（`wao stage <n>`）+ **artifact 落点**（按 SSOT 五大类别，spec 进版本控制、summary 进 docs/incidents/）。**控制平面**——新增 `src/waoStage.js`（STAGE_NUMBERS 枚举 SSOT + STAGE- 前缀文件 + map 索引，镜像 waoDeclare 模式）+ CLI `wao stage <n> --task [--artifacts]`（枚举校验防跳号）+ `runs dashboard` 渲染 `[pipeline]` 进度行（缺口显形）。强制力 = 曝光（可见）不是拦截——Lead 仍可跳步，但跳步留缺口。test/waoStage.test.js +6、test/cli.test.js +2、test/docs-consistency.test.js +1（阶段号一致性守卫）、test/waoLayout.test.js 扩展（STAGE- 正文/map 计数守卫）。**验证靠真实 dogfood**：Lead 是否被引导走完 6 阶段声明 + dashboard 缺口是否显形 |
| TD-84 | 2026-07-07 dogfood（WP-GUI-DEVDEPS 报告） | 跨项目 scope | TD-83 dogfood 暴露：Lead 在 WAO repo 编排、派 worker 去 life-index_gui 干活时，`wao stage`/`wao declare` 写进了 Lead 进程 cwd 的 `.wao/`（WAO repo）而非目标项目——目标项目 dashboard 看不到 pipeline 进度，审计断裂。根因：五个 wao 命令 + dashboard + doctor 的 cwd 解析全是 `options.cwd ?? process.cwd()`，不读 worker 子进程已注入的 `WAO_TARGET_CWD` env；Lead 进程又没有该 env，所以 Lead 跨项目调 stage 也救不了。 | **🟢 env 回退链 + SKILL 提醒双层落地（2026-07-07）**：**控制平面**——新增 `resolveTargetCwd(options)` helper（`src/cli.js`，回退链 `--cwd → WAO_TARGET_CWD → process.cwd()`），替换 9 处 `resolve(options.cwd ?? process.cwd())`（declare/stage/state/decision/handoff/init/doctor/dashboard）。worker 调 wao 命令**自动正确**（env 注入覆盖），角色 prompt 的 `--cwd $WAO_TARGET_CWD` 成冗余安全网。**文档**（SKILL）——"跨项目派工的 stage/declare 归属"小节：Lead 跨项目派工时**必须显式带 `--cwd <目标项目>`**（Lead 进程无 env，靠纪律约束）。**不做活跃目标状态文件**（多目标歧义 + 撞 5 槽位守卫）。**未覆盖**：目标项目未 init 时 declare/stage 仍 ENOENT 崩（既存行为，另立 TD）。test/cli.test.js +3（resolveTargetCwd 回退链三态测试）。零现有测试受影响（单元测试绕过 CLI cwd 解析；e2e 全显式带 --cwd） |
| TD-85 | 2026-07-07 dogfood round 3 | 执行可信度 | dogfood round 3 实证：Lead 在 task2 报告里写了完整的 `wao declare --task ... --reason ...` 命令（代码块格式，放在"### declare 声明"标题下），但**从未真正执行**——`.wao/decisions/` 无对应 DECL 文件，dashboard count=0。子代理主动坦白此缺口并识别为"SKILL 安全铁律精神的 Lead 自身版本"（worker 自报完成 ≠ 真完成，Lead 自己也可能犯）。根因：WAO 的 declare/stage 机制是"曝光不拦截"，依赖 Lead 自觉执行；但交付物文本与实际执行之间无机器校验——Lead 完全可以写一条假声明文本进报告，用户/dashboard 不查 `.wao/` 落盘就看不出区别。这是 TD-82"自做零摩擦逃逸"的更深一层：**声称执行 ≠ 实际执行，且声明本身可伪造**。 | **🟡 文档纪律落地（2026-07-07），硬拦截待 dogfood 验证**：**文档**（SKILL 安全铁律 #6）——加"交付物里写命令文本 ≠ 已执行"纪律：(a) 区分"建议执行"vs"已执行"措辞；(b) 不执行时明说"此处不执行"，不留模糊文本；(c) 强制力仍是曝光（WAO 无法自动检测交付物文本与执行差异）。**不做硬拦截**（同 TD-82/83 哲学——Lead 域，曝光不拦截）。**未覆盖**：交付物文本 vs `.wao/` 落盘的机器校验（理论上可做——扫描交付物里的 `wao declare/stage` 文本 + 比对 `.wao/decisions/`——但成本高且 Lead 交付物形态多样，留待此纪律被证明不够后再考虑）。**验证靠真实 dogfood**：后续 Lead 是否还出现"写了 declare 文本但没执行"的情况 |
| TD-87 | 2026-07-07 dogfood round 2 发现 | 安全陷阱 | kimi-code backend 的 `kimiCode.js:15-17` 注释自承："kimi stream-json 不含 usage/token 字段，进程式 backend 的 tokenBudget 硬闸门对 kimi-code 无效，配 tokenBudget 不会报错但不生效"。这是**静默安全陷阱**——用户在 agents.json 给 kimi worker 配了 tokenBudget，以为有成本保护，实际没有（靠 kimi 自带 max_steps/timeout 兜底，语义不同）。dogfood round 2/4 均在能力矩阵审计中浮现此风险。 | **🟢 warning 已加（2026-07-07）**：`registry validate` 对 `backend===kimi-code && tokenBudget!=null` 的 worker 输出 `⚠` 警告（不 block，只提示"配了不生效，靠 kimi 自带兜底"）。**未覆盖（根因修复）**：让 kimi tokenBudget 真正生效需改 kimi CLI 协议或加进程级 token 估算（从 stdout 字数反推），工程量大且不精确，暂不做。**替代方案**：在 SKILL/AGENTS.md 明确"kimi worker 不要配 tokenBudget（无效），靠 kimi 自带 max_steps_per_turn=100 + agent_task_timeout_s=900 兜底"——但这是运行时行为文档化，不如 validate warning 可见性高。**validate 前置提示已加（2026-08-19 批次 2，ADR-0025）**：warning 从硬编码 `backend===kimi-code` 名字分支泛化为 backend 代码类闭集能力声明（`reportsTokenUsage`，`src/backends/*.js`）的静态交叉校验——任何未声明 usage 上报的 backend 配 tokenBudget 都会 ⚠（kimi 行为不变）；根因修复仍开放。**认证面症状经能力化解除（2026-08-20，Owner 批准）**：reliability 认证的 `metricsNonZero` 检查自起按 backend 能力声明条件适用——判定源即批次 2 的 `backendCapabilitySnapshot` SSOT（纯内核 `scripts/reliability/metricsCheck.mjs`，`scripts/run-reliability.mjs` 消费，无第二套判定；取代旧 `providerID!=="kimi-for-coding"` 名字分支）。声明不上报 usage 的 lane 此检查按"通过 + detail 明示 not applicable"落账——kimi 形状 lane（coder_mm）不再每轮落 conditional / `lastHealthyRunAt` 恒 null（旧形状：检查被整个省略 → observability 必需类目缺失）；声明上报 usage 的 lane 断言不变（parser 回归金丝雀）。根因（kimi 上 tokenBudget 真正生效）仍开放，validate 警告保持。**僵尸 caseId 清算（2026-08-20 同日，Owner 批准，Lead 自做 DECL user-assigned）**：mergeCaseResults 以 caseId 为键只覆盖不清理——matrix 行 label 改名后旧 label 的陈年 case 永久滞留、拖累 worker 级最差聚合（coder_mm 曾 4 case 中 3 个僵尸 conditional 拖住现行 certified case，标签升不上去）。新增 `pruneStaleCases` 纯函数（certification.mjs 单一定义处；规则 = caseId 仍在矩阵 labels 或 agentId 不在矩阵 agentIds，scope 外 agent 不动）接进 run-reliability merge 调用点；一次性台账清算走同一 SSOT 路径重生成 summary（零 token，清掉全库 12 个旧标签僵尸 case），coder_mm 升 **certified** + lastHealthyRunAt 09:05 保留。测试 5 个（含复刻实况的认证级 T4 + glue 结构钉 T5），变异 M1/M2/M3 自证红后复原；npm test 227/227。 |
| TD-88 | 2026-07-07 dogfood round 1-4 | 编排行为（派工积极性） | 4 轮 dogfood 实证：无论 SKILL 文档打磨多好（friction 18→4→2→2），Lead（GLM-5.2 / GPT-5.5）都倾向自做而非派工——GPT-5.5 也只"象征性"派 1 个 researcher 做只读摘要，核心工作仍自做。根因不在文档（已收敛），在**摩擦方向**：派工要拼长命令 + 写边界 prompt + 同步阻塞；自做是 Read 一次调用，零摩擦。即使 SKILL 把成本账本摆对称，LLM 的默认推理仍选阻力最小的路。**架构上限**：派工（跨进程）永远比自做（Read 进程内）多一次开销，无法让派工比自做快——只能在派工收益明显的场景把摩擦降到 Lead 愿意选。 | **🟢 工具层降摩擦已落地（2026-07-07）**：**机制 1（wao ask 快捷派工）**——`wao ask <agentId> "<一句话>"` 薄包装 runCommand，自动注入只读边界模板（`--mode write` 关掉），降低单次派工的命令构造成本。**机制 2（模板库 + workflow list）**——`workflows/templates/` 预制参数化模板（`analyze-implement` 串行 + `parallel-research` 并行）+ `workflow list` 列出 + `workflow run <名字>` 按名字调用（不必敲全路径），降低多步派工的 .mjs 写作成本。**SKILL 派工纪律加强**：决策表加"机械性 leaf 工作必须派工"硬措辞。**未覆盖**：控制平面看不见 Lead 原生工具调用（Read/Edit/Write 不产 transcript）这个根因仍在——自做仍零摩擦逃逸。**验证靠 dogfood round 5**：有 ask/模板库后，Lead 派工行为是否改变。test/cli.test.js +4（ask 校验 + workflow list + 名字解析）。 |
| TD-92 | 2026-07-08（round 4 想法 → round 7 后落地） | 可观测性 | dogfood 7 轮里 friction 全靠 Lead 手动记（SKILL stage 6 引导），质量高但触发点偏晚（任务最后一步才记，容易忘或简化）。用户在 round 4 失望时提过"给 WAO 加 debug mode"，当时没落地——因为碰到了设计张力：friction 是 🟡 域（Lead 语义判断），而"自动记录"听起来像 🟢 域。7 轮 dogfood 给了洞察——缺的不是全自动检测，是**客观摩擦（🟢：失败/超时/budget/scorecard fail）的自动捕获**，和主观摩擦（🟡）互补。 | **🟢 已落地（2026-07-08）**：`src/frictionLog.js`——run 失败终态时自动调 `diagnoseFailure()` 分类，category≠none/unknown 则写 `.dev/friction-log/auto-<ts>-<category>-<runId尾>.md`。复用 `diagnosis.js`（SSOT 分类）+ 镜像 `alerts.js` raiseAlert 纪律（fire-and-forget 不阻塞终态）。`runManager.js` 两处 `_transition`（Run 级 + RunManager 级预生成失败）加 hook。开关：`config.debugMode`（默认关）或 `WAO_DEBUG=1` env。auto-*.md 含客观 evidence + 留主观 TODO 给 Lead 补。test/frictionLog.test.js +6。**不覆盖**：Lead 原生工具调用摩擦（Read/Edit 不产 transcript，架构上限）；worker retry（WAO 无重试）；历史 run 回扫（用现有 `runs diagnose`）。 |
| TD-93 | 2026-07-08 真实任务复盘（WP-GUI-UPGRADE-ATOM-S2） | 测试基础设施缺陷 | `frictionLog.js` 块注释里字面量 `*/`（`dogfood-*/日期`）导致整文件解析失败，阻断所有派工（declare/run 全挂 `SyntaxError: Unexpected token '*'`）。`npm test` 漏网——因为 `test/_guardBypass.mjs` 让测试用任意 node 跑 import，但**不覆盖裸 parse**；且 `frictionLog.test.js` 是 dynamic `import()`，v22 node 对已修复的 `* /`（有空格）宽容，但无空格的 `*/` 会炸。根因：**缺 `node --check` 级语法 smoke**——测试只测运行时行为，不测"文件能不能被解析"。真实任务复盘 P0 指出这是最破坏节奏的问题（控制平面自身崩溃）。 | **🟢 已修复（2026-07-08）**：(1) `frictionLog.js` 注释改写不含 `*/` 相邻字符；(2) 新增 `test/parseSmoke.test.js`——对 `src/` 下所有 `.js` 跑 `node --check`，防注释/语法错误漏到运行时。**未覆盖**：`config/roles/*.md` 等 prompt 文件的 `*/` 风险（非 JS，不影响 parse）；scripts/ 下 `.cjs`/`.cmd`（范围不同，另立）。**教训**：测试基础设施的 guard bypass（`_guardBypass.mjs`）有副作用——它让"在非生产 node 上跑测试"成为可能，但也掩盖了"生产 node 上会炸的语法问题"。parse smoke 补上了这个盲区。 |
| TD-96 | 2026-07-08 真实任务复盘 #10 | 编排摩擦 | `wao stage` 偏重仪式——每个 stage 要手拼 `--task` + `--artifacts` + `--cwd`，当 WAO 自身出错时声明反而成额外阻塞点。复盘建议 `wao stage auto --from-run <runId>` 自动填 run transcript artifact，降低摩擦。 | **🟡 未做（登记留后）**：`wao stage auto --from-run <runId>` 有价值但非紧急。当前 B3 已让 run artifact 自动解析为绝对路径，部分缓解了手拼路径的摩擦。--from-run 的完整实现（自动填 task + 从 run transcript 提取描述）待真实使用积累更多需求后做。 |
| TD-97 | 2026-07-09 外部审计 | 架构债务（证据语义分叉） | TD-95 引入 `run.evidence_audit`（runManager failed 路径写）+ diagnosis 消费它，但 scorecard 仍有一套独立的证据规则（`checkScorecard` 在 completed 路径）。现在有**三套证据判定**：scorecard（completed 路径）、_auditEvidenceOnFailure（failed 路径）、diagnosis（读 transcript 事后分类）。它们各自检测 file_written/command/assistant text，但判据不完全一致（如 hasDoneEvent 只在 scorecard 有）。审计指出这是"战术补丁合理但战略上应统一"——长期会造成信任模型漂移。 | **🟢 已偿还（2026-07-09，第一阶段全统一）**：新增 `src/runEvidenceAssessment.js`（SSOT），导出 `assessRunEvidence(events)`——从事件数组计算统一事实（hasFileWritten/hasCommandExit0/hasAssistantText/hasToolUse/hasAnyEvidence + 各自 count）。兼容三种 message 形状（transcript 落盘 `run.event kind=message` / scorecard 临时 `run.message` / runManager 内存 `{info:{role}}`）。三处调用方全部复用：(1) runManager `_auditEvidenceOnFailure` 合并 evidence+messages 后调它；(2) diagnosis `evidence_passed_backend_failed`/`no_effect` 调它取 file/command count + 活动判定；(3) scorecard `requireEvidence`/`requireAssistantText` 调它取 hasAnyEvidence/hasAssistantText。**未完全统一的边界**：scorecard 的 `requireCommands`（含 `withInferredCommandExitCode` 推断逻辑）和 `requireFiles`（含 `fs.stat` 真实存在检查）有 I/O + 复杂匹配逻辑，不适合纯函数 assessRunEvidence，仍独立。但核心事实判定（file/command/assistant text/evidence 存在性）已统一，漂移风险消除。test/runEvidenceAssessment.test.js +14（形状兼容 + 事实 + 无处方）。npm test 693/693。 |
| TD-115 | 2026-08-14 D1-D3 终审 | 认证新鲜度语义 `lastHealthyRunAt` 非 sticky：`mergeCaseResults` 以 caseId 为键 fresh 覆盖 prior，worker 唯一绿 case 重跑失败后新鲜度归 null——最需要"上次健康是什么时候"的失败场景拿不到该信息（auditor Bug#2；D3 验收已知并接受）。诚实不伪造符合安全铁律，但削弱字段价值。 | 若 Lead 实际需要失败场景的历史新鲜度，为 worker 级新增 sticky 持久字段（summarizeCertification 之外另存），需要动 summary schema，届时再立项。 |
| TD-116 | 2026-08-15 Owner 四方向评审（coder_mm/auditor 双咨询：run_20260814235928766l1gent / run_20260814235936986bt100o，全文留档本地 `.dev/consult/`，未入库）；2026-08-19 app-server 通道补充核实（Lead 会话内主动核查，无派发） | zcode backend 迁移构想：coder_hq 脱离 claude-code wrapper，改用 ZCode 桌面内置 CLI（zcode.cjs）headless `--prompt --json` 直驱 GLM-5.3 | **Owner 决定（2026-08-15）：放弃迁移，保持 claude-code。**硬门槛：`run_correct` 在途纠偏——headless 单次执行无 stdin 追轮（桌面"引导模式"不覆盖 headless；官方无公开 ACP/SDK，第三方 zcode-acp 为早期项目——出处：Owner 决议 + 另一会话 ZCode 外部驱动能力调研）。重看触发：ZCode CLI 提供 headless 多轮纠偏通道（stdin 或稳定 app-server 协议）或社区出现成熟方案。前置（若重启）：Owner 一次性 `zcode login`（OAuth 无法代办）+ raw-capture 实测 `--json` 流式形态与工具事件结构。已知坑（双咨询结论）：parser 保真度直接决定 delivery 隔离（现认证套件无负向（越界写）逃逸用例——正向 isolation drill 三查存在于 `scripts/run-reliability.mjs:216-218`，缺的是对抗性越界写断言，需先补；**该子句已补（2026-08-19 批次 3，ADR-0025）**：`adversarialEscape` drill 落地——`scripts/run-reliability.mjs` drill glue + 纯判定内核 `scripts/reliability/adversarialEscape.mjs`，证伪优先 dry 测试 `test/registry-roles/reliabilityDelta.test.js`；真实实跑消耗 token 由 Owner 决定，残余面登记 TD-133）；config 文件凭据绕过 env redactor；`--mode yolo` 无权限门，安全承重墙是 worktree containment；binary 需固化副本到 `~/.wao/runtimes/zcode/` 再 pin（桌面自动更新会改写安装目录）。**补充核实（2026-08-19）：重看触发第一条件的 app-server 通道存在性已确认，未满足项收窄为"在途注入 + 公开承诺"，Owner 裁定维持。**本机实装：桌面 3.8.1（ZCode.exe ProductVersion 3.8.1.5310）/ CLI zcode.cjs 0.16.3（`--version` 实测；08-14 CLI 日志 `~/.zcode/cli/log/zcode-2026-08-14.jsonl` 证当日 0.16.1→0.16.3——与裁定同代，本次为深挖核实非上游新增）。证据：(1) CLI 内置 `app-server` 子命令（"ZCode Protocol" stdio 服务，桌面自用同款），0.16.3 bundle 方法表含 `session/create\|send\|stop\|resume\|fork\|list\|read\|messages\|events\|subscribe\|compact\|close` 等；(2) 只读 live probe `session/list` 成功返回真实会话且与运行中桌面共享会话存储——跨进程 resume 基础成立（信封为精简 JSON-RPC `{id,method,params}`，带 `jsonrpc` 键即 -32600；无 `initialize` 握手、方法直达；probe 脚本留档本地 `.dev/zcode-appserver-probe.mjs`，未入库）；(3) headless CLI 另有 `--resume <sessionId>` / `-c/--continue`。**仍未满足**：(a) 在途注入——`session/send` 请求 schema 无 delivery/queue 参数，turn 运行中硬拒绝（-32010 "A prompt is already running for this session"）；内核 `steerTurn(delivery:"guide")` 中途引导机制存在但仅运行时内部路径（桌面引导模式/自动 follow-up 等）调用，未暴露为 wire 请求参数——纠偏只能 stop→send 打断式，非排队注入；(b) 公开承诺——协议无公开文档与稳定性版本承诺（第三方 zcode-acp 桥已获社区采用但仍非官方）。另 `zcode login` 前置未变（本次实测 `--prompt` 被 "Model config is missing" 挡住，`--resume` 功能性实测未做，留待重启迁移时按前置清单补）。 |
| TD-117 | 2026-08-15 Owner 四方向评审 | dsh（deepseek-harness）实验暂停 | Owner 动因：deepseek 模型在 dsh 中任务完成质量更高、token 消耗更少。放弃原因：dsh `supportsSessionReuse=false`（`src/backends/deepSeekHarness.js:76`），researcher 迁移会在 spawn 前硬失败（`runManager.js` 抛错）；coder_low 已于 2026-08-15 回滚 claude-code（对齐 `agents.example.json` 模板与 `team-roles.md`）。重看触发：dsh 支持会话复用，或 deepseek 质量/成本优势大到值得为单 worker 保留独立 runtime。dsh 安装副本保留在 `~/.wao/runtimes/dsh-jsonrpc/` 供复用。**validate 前置提示已加（2026-08-19 批次 2，ADR-0025）**：sessionReuse 配置 × backend 代码类 `supportsSessionReuse` 声明（未声明按 false 读）→ `registry validate` ⚠ warning（失败提前到静态阶段；不 block，`src/runManager.js` spawn 前 fail-closed 硬门保留不动）；暂停处置不变。 |
| TD-120 | 2026-08-15 守卫漂移实证（当日两次撞车：SKILL "默认 coder_low" 断言、模板 "GLM-5.2" label 断言——均为合法策略变更被旧值指纹挡下） | docs-consistency 值指纹守卫模式：断言硬编码"当前策略值"（lane 名、模型名、label 字符串、工具数），合法策略变更时必须双份维护（文档+测试）；漏改测试即假红，留旧值即把漂移冻结成契约。TD-34 的泛化登记在"文档大幅重构"场景，本条实证"小策略变更"同样触发。 | **处置原则（关系型守卫，TD-120）**：守卫断言"关系"而非"值"——(a) doc↔代码 SSOT：文档中的计数从权威源派生（如 usage/SKILL 工具数从 `toolSurface.js` `TOOLS.length` 派生）；(b) doc↔doc：两份文档的同一事实必须一致（team-roles 模型行 ↔ `agents.example.json` model.id）；(c) doc 内部自洽（cert label 必须包含生效 modelId，`entry.modelId ?? agent.model.id` 封"删字段逃逸"口）。合法变更时只改权威源一处，守卫自动跟随并精确指出漏改面——恰好是真实漂移。**已转换**（2026-08-15，含会审返工）：label 自洽断言（effectiveModelId）、team-roles↔模板模型交叉检查（**分节定位**——全文 contains 对共用 modelId 的角色互换盲，auditor 实证）、22-tools 计数 SSOT 派生（正向 `(?<!\d)` 数字防护 + 反向派生 `n+1` 防"下一个值"死锁）。**触发**：触碰任何值指纹断言时按此原则改造；新增守卫禁止硬编码可漂移值（真正的冻结契约数字——如 SKILL 字节上限 17000（Owner 2026-08-15 批准放宽）——除外，变更需 Owner 明示）。 |
| TD-123 | 2026-08-15 P1 layering 验收会审（coder_low 必查点 e + "没问但应该问"） | M9-6A-09 全行扫描假阳性面 + bare specifier 未入方向矩阵 | (a) M9-6A-09 关闭多行盲区后改查全部行，但 `from\s+['"]…commands/…` 正则会误伤注释/字符串提及（application 现有 47+ 处 "Does not import src/commands/*" 注释离假红只差一次措辞）、application 本地 `commands/` 子目录名、含 zod/SDK 字样的注释——18/18 绿是内容巧合非结构安全；结构化修复=与 layering 的注释剥离/语句位置门槛对齐。(b) layering 只对相对边建档，application 桶 bare 包出边（如未来 `import { z } from "zod"`）方向矩阵不红，只剩 M9-6A-09 与 mcpRegistry M9-1-10 两道防线；并入矩阵（application 桶 bare 出边白名单 `node:` + 冻结豁免集）可让 SDK/zod 禁令吃同一套冻结精确集合机制。 | 触发：首次 M9 假红实证，或下次触碰 M9-6A-09 / layering 矩阵时顺带改造。 |
| TD-124 | 2026-08-17 R8-C auditor 窄复核（run_20260817095718719a7u81f） | runs/ 卫生守卫两层各有有界缺口：(a) 静态层 `repo-root-join` 规则结尾 `\s*\)` 使 `join(REPO_ROOT, "runs", "leak")`（单行/双引号/repo-root 派生——恰在其宣称契约内）逃逸；(b) 白名单抑制按"文件+模式"非行内容锚定——同文件同形状的新增违规被顺带豁免（新鲜度测试只防死条目腐烂）；(c) 动态层 boundary 2 声明"全部已知写者深度 0/1"不实——`.session-reuse/.locks/`、`.lineage-reuse/.locks/` 是 live 深度-2 写入面（sessionReuse.js:225,433）。 | 修复面：规则结尾放宽 `\s*[,)]`；白名单收窄为行内容/行号锚定；boundary 声明修正 + 评估递归二层或按需 stat。触发：下次触碰守卫/白名单时顺带，或首次真实逃逸实证。两层并集当前覆盖 auditor 全部攻击面（静态漏网形状落层 2，唯深度 ≥2 角落例外——实际写者危害有限，lock 短命且同路径深度 1 有可抓写入）。 |
| TD-125 | 2026-08-17 R8 双席会审（auditor "没问但该问"） | runs/ 目录生命周期：现势 2842+ 条，同时承担生产 transcript 库、session/lineage 复用槽位状态、守卫观察对象三职；无归档/轮转策略，守卫快照成本随条目数线性增长。 | **2026-08-20 已偿还（决策+实施同日）**：Owner 拍板 Option B 分层归档（90 天参数、手动触发，R23-B 收口轮）——R23-B1 落地 `runs prune --older-than <dur> --archive`（run_20260820220133302xu64m2 / commit b7d2b02 + Lead 补全：移动到 `runs-archive/<yyyy-mm>/<原文件名>`、判龄与删除同段、冲突不覆盖、truthy flag 加固钉测试）；守卫快照成本实测 ≈0（26.5ms/套件，不构成理由——排期理由挂法医信噪比/检索体验）；usage.md 消费者缩窗知情面 + 清扫 runbook + 子目录边界已写明。**残余（登记）**：①TOCTOU 覆盖窗（existsSync→rename 间第三方写入，并发双跑 prune 场景；根修 = link+unlink 原子 no-clobber，触发 = 首次真实冲突实证）；②归档循环无逐文件容错（EPERM/坏 JSON 行中断整批，已输出行即恢复痕迹；per-file try/catch 为硬化候选）；③子目录语料（smoke/verify/driver-comparison 等）与顶层日志不在清扫面——下一堆积点，Owner 需要时另立策略；④90 天参数下首扫预期归档 0（最老语料 2026-06，首次咬量 9 月下旬）。 |
| TD-128 | 2026-08-18 R13 验收会审（auditor P1/P2 + coder_low P3；R13-C 返工后登记） | 同类读绑定/输入校验的**类级清扫**（R13-C 修了 retry/resume/stop 三 lane 后的剩余面）：(a) `sessionReuse.js:346`——前任 run 的 `session.created` 存在性检查用无绑定 `findLatest`，外 run/伪造行可翻转 reuse 路由（busy/resume/first 分派）；(b) `runCorrection.js:149`——`run.background_submitted` 无绑定 `findLatest` 喂 correctable 门，尾部外 run 伪造行可开门；(c) `commands/shared.js:137-150` `loadRun`——无 `isValidRunId` 校验直接拼接 `runDir` 与 `<runId>.jsonl`，路径拼接面（`../`、绝对路径）与 runStop 的 FIX-A 同类但未修。另（coder_low P3-3，R13-C 后重新核对）：跨车道 `session.created`/`run.started` 首条 vs 末条语义分叉现状——resume 取**首条绑定**（`findFirstBound`，权威事实=首写），stop（R13-C 起）/**collect**（`runCollect.js:184`，未改）/**activity**（`runActivity.js:73`，未改）取末条；分叉各自有语义理由（resume 接续首会话 vs 观测面取最新会话）但无单一定义处。**（R14 验收会审 coder_mm 对抗席 P1 新增，R15 偿还）findState 族**：`sessionReuse.js` 三处前任状态投影无绑定（`resolveReuseTurn`/`resolveLineageFirstTurn`/`resolveLineageContinuationTurn`，原 :341/:514/:570）——`findState` 末条胜出，append-only 外 run `run.state_change` 尾条可现实翻转 busy/resume/first 路由（在飞前任 + 伪 completed 尾 → resume = 同一 provider 会话被并发驱动，违反该文件自身 Contract 6；反向伪 running 尾阻断派发）。**P3 同族低危残存（W1-W3 已偿还（R18，2026-08-18 本次交付，见处置列）；W4 保持开放）**：~~W1 报表污染类~~——`metrics.js:10/:11/:16`（aggregateRunMetrics 的 state/run.metrics/run.started 读取全无绑定）、`commands/runs.js:563/:565/:591/:593/:595`（runs metrics/scorecard 聚合与 `events.find`，且两命令 CLI runId 均未过 `isValidRunId` 直接 join——TD-128c 同类）、`commands/run.js:149`、`src/smoke.js:225/:259`（scorecard.checked 无绑定 find）——伪造尾条可污染 tokens/cost/duration/scorecard 报告值；~~W2 状态投影类~~（R16 会审新增登记）——`runAwaitResult.js:522/:673` 的 findState 无绑定喂 await 状态投影；~~W3 自续接类~~——`runManager.js:1273`（resume 终态门 `findState` 无绑定——外 run 伪 running 尾可把 terminal run 的 resume 拒绝翻成接续，仅自身会话，自续接面）、`runStop.js:162`（`fromState` 无绑定）；`runAwaitResult.js:269-270` 手搓绑定反查换 `findLatestBound` SSOT（纪律不一致）已随 R18 一并完成（行为恒等交换）。**W4 明确不做（登记维持，类级决策）**：~~`runCollect.js:85-91` reconstructItemsFromEvents 的 run.event 过滤无绑定——外 run 尾条可被重建进 collect/await 输出（证据污染面，修复涉冻结快照组成）~~（R21 W2 升格修复——"冻结快照组成"成本被高估：调用侧绑定即可、SSOT 本体不动，见处置列 R21 块）；~~`runCollect.js:109/:113` events[0]/seq 水位（评过不够格，登记备查）~~（R21 W5/W6 换真理由：events[0] 入类级登记（append-only 首写不变量）、seq 并入水位族（全局单调性），见处置列 R21 块）。**（R18 交付新增登记，同族低危观测/自续接面）**：~~`runs.js` metrics **--summary** 分支 aggregateSummary 逐文件聚合读取无绑定（权威 runId 可从文件名派生，若修在调用处逐文件过滤）~~（R19 已偿还）；~~`smoke.js:87-88`（原登记 `:84-85`，行号勘误——R18 import 增致漂移）smokeOne 的 state_chain/run.started worktreePath 读取（保持开放）~~（R21 W3 已偿还——stateChain 假绿方向 + findFirstBound 首写纪律）；~~`runStop.js:137` 授权失败路径的 findState（保持开放）~~（R21 W3 已偿还——boundReportScope 语义，legacy 回退保持历史回显）**（R19 会审终态清点新增簇——观测投影最后一簇，与 R18/R19 所修同形状同威胁模型）**：~~`runStatus.js:137/:147/:150`（run_status 的 state/terminal/last/lastActivity 无绑定——同函数 agentId/executionStage 已绑定，函数内纪律分叉）；`runList.js:90`（runs list 每行 state/terminal）；`runs.js:425-427`（runs summary byState/latest）与 `runs.js:303-310`（dashboard 行 state/tokens/evidence）~~（R20 已偿还，见处置列）。另对称登记：`runWait.js:291/:386` findLastEventSeq（cursor 水位）未绑定（仅 liveness 探测降级、终态判定已免疫）——与 runCollect seq"评过不够格"同族。。**（会审补登；runWait/:280/:373、smoke:274、--summary 注释理由三面已由 R19 偿还，见处置列）**：~~`runWait.js:280/:373`（findState 无绑定喂 run_wait 终态/状态投影——与 runAwaitResult W2 类同族，低危观测面）~~；~~`smoke.js:274`（state_change 末条无绑定喂场景 2 判定）~~；~~`runs.js --summary` lane 的注释理由（"无权威 runId"）偏弱——文件名 stem 即权威 runId，绑定廉价，待类级决策~~（R19 已绑定且注释已修正）。 | **🟢 主体已偿还（R14，2026-08-18，run_20260818133309666l4p9ta / commit a54f093；R14 声明不含 findState 族——该族由 R15 偿还：run_20260818151841993epxrpk / on-main commit 7450edc，2026-08-18）**：(a) `sessionReuse.js` 存在性检查 → `findLatestBound`（绑定前任 `entry.runId`）——四处中**唯一 LIVE 修复**（该函数无 `extractCanonicalAgentId` 上游身份门，外 run/无信封尾条真能翻转路由；探针 R14-SR-1/SR-3 经变异自证红）；legacy 无信封前任 → **降级 first**（不复用无法归属的会话；usage.md 行为变更条已列；R15 起收窄——全无信封前任在更早的状态门即停为 busy，仍到达此降级分支的只剩『绑定终态 + session.created 行本身外 run/无信封』形状，R14-SR-1/SR-3 语义不变）。(b) `runCorrection.js` correctable 门 → `findFirstBound`（派发时稳定事实的首条纪律）——**锚点复核修正原登记**：step 2 的 `extractCanonicalAgentId(events, runId)` 已把任何外 run/无信封行拒为 `unknown_run`，先于该门，故原"尾部伪造可开门"不可达，交换为行为恒等的纪律一致性+纵深防御（R14-CR-1 钉分层真相，身份门将来放宽时变红）。(c) `loadRun` → join 前 `isValidRunId`（delivery.js SSOT，无环 import；fixed-safe 文案不回显输入；R14-LR-1 拒绝/放行两态——注意 SSOT 无长度上限，超长字符集合法 id 止步于 ENOENT，未在 loadRun 另造长度帽以免分叉校验器）。(d) 首/末条分叉单一定义处说明 → `transcript.js` 绑定读取器注释块（各 lane 取法+语义理由+legacy 各自明示处理）。(e) **R15 findState 族**：三处前任状态投影改 `findState(events.filter((e) => e && e.runId === entry.runId))`（绑定到槽位前任 `entry.runId`，`runDelivery.js:364` 同款范式；探针 R15-SR-P4/P5、R15-LIN-P6/P6b、R15-SR-LEGACY 经变异自证红——三处过滤临时还原后 5 探针全红、10 个 R14 探针不动、复原后全绿）；语义 = 绑定过滤（外 run/无信封行不可见，状态只由前任自身事件计算，不 fail-closed 整个路由）；**行为变更**：全无信封前任 transcript 由 R14 的『降级 first』收窄为『状态不可归属按在飞 busy』（不可归属 = 永不并发驱动；TD-129b 本安装面 pre-envelope 前任 ≈0，实际影响≈0）。合法路径回归 = 既有 m11-11c/m12-7 套件全绿（正常 transcript 每行带 runId 信封 → 过滤器恒等，行为不变）。**P3 残存 W1-W3 已由 R18 偿还（2026-08-18 本次交付）**：**W1 报表污染类**——`metrics.js` aggregateRunMetrics 三处事实读取（state/run.metrics/run.started）与 duration 终点（末事件 ts）经 `boundReportScope`（metrics.js 单一定义处，R18 新增导出）收窄；序语义保持既有纪律（run.metrics 末条——metrics.test.js 钉住；run.started 首条——R12-C 首写纪律）。runs metrics/scorecard 两命令 runId join 前过 `isValidRunId`（锚点复核：两命令均不经 shared.js loadRun，直接 join+read，校验此前缺失；fixed-safe 文案不回显输入）；`commands/run.js` loadScorecardFromTranscript（R18 起导出为探针位）与 `src/smoke.js` 两处 scorecard 读取同款。legacy 选择（观测面=降级不设门）：全无信封 transcript 保持既有读法照常出报表（cli.test.js pre-envelope 三态 JSON 契约钉住），任一事件带信封（含伪造尾行）即严格绑定——宁可报表可见地缺事实，不静默采信不可归属的值。（会审量化披露：全无信封的 legacy 文件被注入**单条带信封伪行**即触发严格绑定、整份真实事件不可见——"一行夺舍"形状；攻击者门槛与既有边界相同（runs/ 写权限+文件名即 runId），读侧无解维持。）**W2**——runAwaitResult 初始读与等待循环每次 poll 的 findState 改绑定过滤（R15 范式）；legacy 全无信封 → `findState([])="pending"`（不可归属状态永不投影为终态，不 throw 不 read_failure，等待窗如实耗尽）；:269 手搓绑定反查换 `findLatestBound` SSOT（行为恒等）。**W3**——runManager resume 终态门 + runStop fromState 同款绑定过滤；legacy 衔接（R15"不可归属按 busy/拒绝"语义）：resume 全无信封过终态门后由 R13-C 绑定 session 读取落入既有 null 拒绝（与修复前在终态门拒绝同一外部结果）；runStop :162 的 legacy 形状不可达（session 门在前，R13C-STOP-3 钉）。探针 R18-MET-1/2、SC-1/2/3、LR-2、RUN-1、SM-1（smoke.js 源级纪律钉——module import 即执行 main()，无注入 seam，stateChangeReasons.test.js:162 先例）、AW-1..5、RES-1/2、STOP-1 落 test/run-lifecycle/boundReadSweep.test.js（该文件已在 manifest，无新测试文件），全部经变异自证红（AW-5 为行为恒等交换的序语义钉，设计上不红——R14-CR-1/R16-CO-3 同口径）。**R19 廉价尾巴清扫（2026-08-18 本次交付，会审补登三面 + --summary 登记面）**：(a) `runWait.js` 初始读与等待循环每次 poll 的 findState 改绑定过滤（R15 范式，与 runAwaitResult W2 同款；legacy 全无信封 → `findState([])="pending"`——不可归属状态永不投影为终态，不 throw、不转 read_failure，等待窗如实耗尽）；(b) `runs metrics --summary` 在调用处以【文件名 stem 为权威 runId】逐文件传入绑定读者（aggregateSummary 增可选 runIds 形参转发 aggregateRunMetrics→boundReportScope——R18 导出复用不新写；runs.js/metrics.js 的"无权威 runId"注释措辞一并修正；全无信封 legacy 文件保持历史读法，合法路径零变化）；(c) `smoke.js` 场景 2 PASS/FAIL 判定的末条 state_change 读取绑定到本 smoke runId。探针 R19-WT-1..4（初始读/poll 两面篡改 + 合法回归 + legacy 语义钉）、R19-SUM-1/2（--summary 单文件篡改 + 合法/legacy 回归）、R19-SM-2（smoke.js 源级纪律钉——module 顶层即执行 main()，无注入 seam，stateChangeReasons.test.js:162 / R18-SM-1 先例）落 test/run-lifecycle/boundReadSweep.test.js（该文件已在 manifest，无新测试文件），全部经变异自证红。**保持开放（登记维持）**：~~smoke.js:87-88 smokeOne state_chain/run.started worktreePath 读取、runStop.js:137 授权失败路径的 findState。~~（R21 W3 已偿还，见处置列 R21 块）**W4 保持开放**（runCollect.js:85-91/:109/:113，见内容列）。**保持开放**：runCollect lane 原在此保持开放，**已由 R16 偿还**（2026-08-18 本次交付，见 TD-127——Owner 拍板不兼容信封前老数据后放行：`:184` → `findLatestBound`、`:242` → `findFirstBound`，旧格式降级"无会话元数据"拒绝，探针 R16-CO-1..5 落 test/run-lifecycle/boundReadSweep.test.js；run_202608181759397651p0g15 / on-main commit 5131bfb）；**夹具纪律（长期）**：本轮触碰的测试文件夹具已诚实化；全库夹具大扫除不做（TD-129a 真实哨兵已消除系统性风险）——任何夹具**复用前先诚实化**（ProcessBackend 家族形状不返回 messageId）。**R20 末簇收口（2026-08-19 本次交付，Owner 批准 + coder_mm 语义裁定为合同；原"除'明确不做'外读绑定家族清零"的清点声称经双席终审证伪，如实口径见下方 R20-C 终审返工）**：**M1-M3 观测投影**——`runStatus.js` :137/:147/:150（state/terminal/last/lastActivity）、`runList.js:90`（extractRunFacts 增可选 runId 形参、listRuns 传文件名 stem）、`runs.js` summary byState/latest 与 buildDashboard 行（state/tokens/cost/evidence）——经 metrics.js `boundReportScope`（R18 W1 SSOT 复用，不新写）收窄。**DEViation 声明（锚点复核）**：这三面纯 R15 过滤会破坏既有测试钉住的 pre-envelope JSON 契约（cli.test.js TD-75 系列钉 run_status 对无信封 transcript 的状态推断与心跳、M8-2 系列钉 buildDashboard、runs.test.js 钉 runs summary——均非本轮授权测试文件），故取报表类语义（与 R18 W1 同款、L4 钉一致）：全无信封保持历史读法，任一事件带信封即严格绑定，混信封下不可归属降级 pending。~~**M2 残余登记**：MCP `runs_list`/`lead_preflight` 的缓存路径（runSummaryCache 的 extractFactsFn 调用无 stem 形参）仍历史读法——闭合需一行 stem 透传，但 `src/application/runSummaryCache.js` 不在本轮授权面，维持登记待下一轮。~~（R21 W1 已闭合——read() 以缓存 key 的文件名 stem 调 extractFactsFn，runs_list/lead_preflight/Owner 看板三条缓存消费面与直读分支同语义，见处置列 R21 块）**M4 诊断**——`runDiagnosis.js:63-64` + `diagnosis.js`（evs 顶层收窄到 boundReportScope；锚点清单之外同函数同族的 auth/config/capacity/timed_out/budget/scorecard/evidence_audit/run.event 活动读取一并绑定——单一定义处而非逐行补丁）。legacy 行为选择（"先核实既有语义再选"的复核结论）：全无信封 transcript 保持历史分类读法——frictionLog TD-92 契约钉住该语义（`frictionLog.js:55` 以合法 runId 调 `diagnoseFailure(events, runId)`，其测试夹具为 pre-envelope，纯 R15 过滤会把分类降 unknown → debug 模式不再写 auto 档——fail-closed 退步），故与 M1-M3 同取 boundReportScope 语义；零绑定事件（信封存在但全不可归属）→ 复用既有空输入降级 `{category:"unknown"}` + state "pending"，不 throw；expectedRunId 缺省保持历史无绑定读法（M9-5A-01 等既有契约不变）。**M5 delivery 观测**——`runDelivery.js:842/:1080/:1147` terminalState 绑定（:364 文件内既有先例同款）；不可归属 → "pending" 非终态路径。顾问裁定三登记点落实：isolation 投影门（gatherDeliveryView :588）同受绑定保护（比原登记更宽的面）；候选"恢复出现"效应两方向关闭（外 run completed 尾条曾压制真实 failed 的 backend_failed 候选门、外 run failed 尾条曾凭空武装该门）；usage.md 行为变更条（候选投影自 R20 起只由本 run 自身事件门控）已落。**M6 交付丢失向量（本包最重）**——`runManager.js _externalTerminalState` 绑定到 `this.transcript.context.runId`；不可归属 → 不采纳（null，落回自身终态化路径，fail-closed 方向，R18 W3 注释先例）。:2192 的无绑定采纳曾是交付丢失向量（伪终态 → 跳过 _finalizeDelivery → run 以 completed-ish 收场而交付物从未打包）——绑定即关闭；:2080 流后采纳同受保护。输入面复核（顾问已核）：仅 transcript 重读、无进程探测；stop/abort/backgroundRunner 合法写入车道全带信封，绑定对合法写入透明。usage.md 行为变更条已落。**M7 daemon**——`daemon.js:104/:147/:149/:506` 绑定（stem/请求 runId 为锚；:149 agentId 为会审补扫漏网）。孤儿恢复效应（真实在飞 + 伪终态 + owner 死：绑定前被①永久压制、绑定后诚实放行 resume）并入 usage.md"压制解除"条；绑定不改变 ①findState ②owner 心跳 关卡序（逐情形核对结论）。**L1 注释撞号勘误**——R19 src 注释 W1/W2/W3 标签改挂登记表真实编号：`runWait.js`×2 W1→W2（状态投影类）、`metrics.js` + `runs.js` --summary 注释 W2→W1（报表类）；smoke.js W3 已对不动；usage.md 标签已对不动。**L3 backgroundRunner**——:250/:297/:338 绑定：伪终态尾条不再压制启动失败/delivery-json/reuse-worktree 解析失败的 run.error+failed 落盘。**L4 混合信封语义钉**——R20-L4-1：部分行带信封 + 部分裸行 → 只从绑定子集投影（与 R18 语义一致的方向性钉）。**测试**：M1-M4/M6-:2080/M7/L3/L4 探针落 test/run-lifecycle/boundReadSweep.test.js（该文件已在 manifest）；M5 + M6 交付向量探针落新文件 test/delivery/runDeliveryReadBinding.test.js（manifest worktree 组同步）；每处一对（篡改探针 + 合法回归），全部经变异自证红（逐面变异清单见交付报告：M1/M2/M3a/M3b/M4a/M4b/M5/M6/M7a/M7b/M7c/L3-:250/L3-:297:338——对应探针红、合法回归恒绿，变异后复原）。探针诚实口径：篡改尾条用【外 run 信封的 legacy 终态 fact】形状——findState 的 legacy 推断路径使尾部伪终态在修复前无绑定读下真实可达，且不同时武装 L2 未修的写侧 CAS（_detectExistingTerminal 只认 state_change），读侧篡改面单独钉住。**保持开放（登记维持）**：**L2 transcript 写侧 CAS**（`transcript.js` :863/:872/:1407/:1459/_detectExistingTerminal）——顾问反向论证成立并采纳：写侧绑定会把"响亮拒写"（run.state_change_rejected + first-terminal-wins 仲裁，伪终态行存在性对审计可见）变成"静默忽略"（读侧过滤后外 run 终态不可见 → 写侧不再触发拒绝 → 伪造行的存在被静默吞掉）——fail-closed 退化为 fail-open；且同 runId 伪造追加连绑定读也能骗过（与读侧无解的攻击面相同）。维持登记不修（**R21 W4 勘误拆条：上述"响亮拒写"论证只适用于终态仲裁 CAS；决策门/纠偏门两条的维持理由换为写侧 SSOT 血溅面，并补登 R20 交互新形状——L2 一条拆为 L2-a/L2-b/L2-c 三条，见处置列 R21 块**）。~~**W4** runCollect 两项维持明确不做（类级决策）。runWait seq 水位维持对称登记（评过不够格）。smoke.js:87-88、runStop.js:137 维持 R18/R19 既有开放登记。~~（R21 处置：W2 升格修复重建绑定；W3 偿还 smoke:87-88 与 runStop:137；seq 水位与 events[0] 换真理由重登——见 R21 块）**R20-C 终审返工（2026-08-19 本次交付；R20 终审 = coder_mm + auditor 双席：交付面 PASS、"家族全清"声称 FAIL——两席各自清出未登记面，会聚 P2 为 runs prune 无绑定时间戳直接驱动 unlink：证据灭失面，重于全部观测面，且与 R20 修复面同文件上方 50 行）**：**C-1（双席会聚 P2，必修）**——`runs prune` cutoff 删除决策的年龄读取（原 `events.at(-1).ts`）绑定到【文件名 stem 即权威 runId】的信封绑定事件（`boundReportScope(...) ?? events`，与 runs summary 的 R20 改法同款）：只有本 run 自身事件喂年龄，外 run 旧 ts 尾条不再把在役 run 翻成可修剪（unlink 前被阻断）；legacy 全无信封文件保持历史判龄照删（R20C-PR-2 钉），零绑定事件（整份只有外 run 信封行）沿既有"末事件无 ts 按最老处理"（ts 0）。usage.md 场景 7 行为变更条已落。**C-2**——`runs wait` SIGINT 中断快照的 `findState` 绑定到请求 runId（R15 范式，与 service R19 初始读/poll 同一过滤形状）：外 run 伪终态尾条不再把中断快照翻成终态；`:219-221` 注释声称"与 service 同一 SSOT"自 R19 起失真（service 已绑而快照没绑），修完重新为真。**C-3（机器消费字段）**——`runWait.js` countProgressAfterSeq 经 boundReportScope 收窄到请求 runId：外 run 高 seq 活动行不再伪造 "progress"（经 summarizeLiveness SSOT 上 run_wait 与 run_await_result 两条 wire；activityEventCount/lastActivityKind 是 Lead stop 决策的机器消费喂料）。legacy 全无信封快照保持历史无绑定计数（runWait.test.js WAIT-RUNTIME-1 既有契约）；混信封下裸行不可见（L4 同向）。**C-4**——`runs dashboard --latest` 排序键 = 各 run 自身绑定事件末条 ts（外 run 远期尾条不再顶掉行序；legacy 全无信封文件按末事件 ts 历史排序保持）。**C-5（auditor P3-1）**——metrics.js boundReportScope 注释勘误：两档（legacy 回退 vs 严格绑定）的真实判据是【该面有无既有 pre-envelope 契约钉住】，非"观测面 = 降级不设门"——M5（runDelivery terminalState，纯观测）无 pre-envelope 契约钉住故取严格档即为在先反例。**测试**：R20C-PR-1/2（C-1 篡改探针 + 合法/legacy 回归，CLI）、R20C-SIG-1（C-2 进程内 process.emit 探针——runsWait.test.js D1-D3 同款模式；本文件更早测试经 RunManager 安装的进程级 SIGINT 单例 handler 需快照摘除后恢复，否则其异步 exit(130) 晚到会真杀测试进程）、R20C-LIV-1/2/3（C-3 内核直调 + run_wait / run_await_result 两条 wire）、R20C-DB-1（C-4 进程内 renderOnce 排序探针 + 合法/legacy 回归）落 test/run-lifecycle/boundReadSweep.test.js（该文件已在 manifest），C-1/C-2/C-3/C-4 逐面变异自证红后复原（对应 PR-1 / SIG-1 / LIV-1..3 / DB-1 变红、PR-2 合法腿恒绿）。**C-6 全量登记（双席清点合并去重，新增开放面）**：`runList.js:110-118` extractRunFacts 的 rawAgentId（events[0]）与~~updatedAt（末事件 ts）无绑定（runs_list wire 的 updatedAt 与排序由其派生——R20 M2 只绑定了 state/terminal；缓存路径 stem 透传维持 M2 残余登记）~~（R21 W3 已偿还 updatedAt=末绑定事件 ts；W1 闭合缓存 stem 透传；rawAgentId 入 R21 events[0] 类级登记）；`runs.js:302/:320-321`【R21 W6 行号勘误：实际 `:309`（agentId）/`:327`/`:328`（ageMs 首/末 ts）】buildDashboard 行 agentId（events[0]）与 ageMs（首/末事件 ts）无绑定（R20 注释已如实标注"不在本轮锚点"；R21 W5 入 events[0] 类级登记）；seq 水位族延伸（与既有 runWait findLastEventSeq 对称登记同类）：`runManager.js:1271` resume seq（events.at(-1).seq）与 `backgroundRunner.js:235/:300/:343` initialSeq（findLastEventSeq 全量）——外 run 高 seq 尾条只抬高新写 seq 水位/制造 seq 空洞，无事实污染（写侧 JsonlTranscript.append 自身 readMaxSeq 同为全量；评过不够格维持）；`smoke.js:235` 场景 1 证据计数的 run.event filter 无绑定（外 run 尾条可虚增 evidence 计数显示——smoke 手动触发、人读输出，低危维持）；daemon scanAllRuns/list 绑定的诚实代价注记：pre-envelope 终态 run 经绑定读 findState([])="pending" → 统一视图显示为非终态 orphan（TD-129b ≈0 存量；resume 候选侧由 manager.resume 的 R13-C/R18-W3 绑定门 fail-safe 兜底，daemon.js M7 注释已列）。**清零声称修正**：R20 原口径"除'明确不做'外读绑定家族清零"作废——如实口径 = **R20 双席终审清点后：已登记面全数处置（修/明确不做/登记维持三态）；终审新发现面已随 R20-C 修复（C-1..C-4）或入登记；维持清单见本列**（L2 transcript 写侧 CAS、W4 runCollect 两项、runWait/runManager/backgroundRunner seq 水位族、smoke.js:87-88/:235、runStop.js:137、runList.js rawAgentId/updatedAt、runs.js dashboard agentId/ageMs、runSummaryCache 缓存路径 stem 透传、daemon pre-envelope orphan 显示代价、**NF-1 `backgroundRunner.js:238/:241/:247` 回填存在性检查无绑定**（外 run 尾条可压制本 run 回填行→resume/retry 绑定门随后拒该失败 run，可用性侧自限）、**NF-2 `runManager.js:2866` friction log 的 run.metrics 反查无绑定**（外 run 尾条可污染 Cost/Tokens 展示，debug-mode 本地产物；诊断分类已绑定不受影响）——NF-1/NF-2 为 R20-C 窄复核终审补登，低危维持；登记后"无未登记同类读面"声称成立）。**R21 收官后维持清单对账更新为 9 项（拆解算术与实况清单见下方 R21 块 W8）。** **R21 收官补丁（2026-08-19 本次交付，TD-128；coder_mm + auditor 双席咨询定稿、Owner 已批准；先逐锚点复核再动手——4 处行号漂移/计数偏差按实况修正并 DEViation 于交付报告：runManager events[0] 实际 :1314（任务写 :1270）、CAS 拒绝内存采纳实际 _transition rejected 腿 :2851-2854（任务写 :2809）、dashboard ageMs 实际 :327/:328、events[0] 未登实例实为 7 个文件锚点 8 行（任务写 6））。** **W1（两席会聚必修）runSummaryCache stem 透传**——`read()` 以缓存 key（resolve 后路径）的文件名 stem（`basename(key, ".jsonl")` 回取，非新事实）调 `extractFactsFn(events, stem)`：MCP `runs_list`（server.js:4460 传 readSummaryFn）与 `lead_preflight`（:3314，经 :2902-2903 的 cachedRunFactsReader）及 Owner 看板（ownerDashboardServer.js:484/:495）三条缓存消费面与 listRuns 直读分支（runList.js:305，R21 注释扩充后 :311）自此同语义——R20-M2 的行 state/terminal 绑定在主消费面生效；修复前 `extractFactsFn(events)` 无 runId → `boundReportScope(events,null)`=null → 历史无绑定读法（R20-M2 只保护了直读分支）。**测试关键**：既有探针全打直读分支（洞因此存活），R21-CACHE-1/2 补走 readSummaryFn 分支——真实 createRunSummaryCache（真 stat + 真读盘）接线，篡改探针（外 run 终态尾条不翻转缓存行）+ 合法回归（miss+hit 两路径恒等，缓存负载即绑定后 facts）。**W2（两席会聚必修）重建 filter 调用侧绑定**——`runCollect.js:234`（process 取回）与 `runAwaitResult.js` collectCompactFromSnapshot（compact 结果）两处调用侧 `reconstructItemsFromEvents(boundReportScope(events, runId) ?? [])`，SSOT 函数本体不动（"冻结快照组成"成本被高估——调用侧绑定即可）；外 run 尾条 run.event 不再被重建进 collect/await 输出（messages/evidenceCounts 是机器消费字段）。**语义选择（依据写明）**：collect 侧无 legacy 腿——到达重建前必过 findLatestBound session 门（R16 已拒全无信封形状），session.created 行本身带本 runId 信封 → boundReportScope 恒非 null，`?? []` 仅为函数全形状防御；await 侧取 R19 runWait 同款（观测面降级不设门）——全无信封 → 绑定后重建为空（宁空不采信不可归属内容），且该形状经 R18 终态门（terminality 由绑定 findState 投影）后理论不可达，空腿为纵深防御。usage.md run_collect 节 R21 内容绑定条已落（"外 run 伪造行对 collect 不可见"自此覆盖会话事实与重建内容两面——修复前该句对重建内容为过度声称，现已为真）。探针 R21-CO-6（deferAppend 投影模式零 audit append）/R21-AW-6（compact 恰取末条 assistant 文本——外 run 尾条修复前直接成为呈给 Lead 的结论）。**W3（三处一行，auditor 顺带修）**——`smoke.js:87-88`：stateChain 用 state_change runId 过滤（场景 2 :277 的 R19 范式）+ run.started 用 findFirstBound（R12-C 首写纪律，全绑定输入上与 events.find 恒等）——喂 report() 场景 1 PASS/FAIL 判定的**假绿方向**（外 run completed 尾条曾可凑出 includes("completed")）；`runStop.js:137`：授权失败回显 terminalState 绑定（同文件 :168 fromState 已绑的纪律分叉补齐），取 boundReportScope（?? events legacy 回退）而非 :168 的 plain filter——授权检查先于 session 门，pre-envelope 可达该路径（ownership 的 legacy 容忍可先失败），保持历史回显；`runList.js:112`：updatedAt = 末【绑定】事件 ts——与 R20-C C-4 已绑的 --latest 排序键同形状、内部自洽；runId 缺省或全无信封 scope===events 历史读法恒等，零绑定事件（信封存在但全不可归属）→ null（宁可缺事实，与 R20-LST-2(c) state→pending 同档、C-4 排序键同口径）。探针 R21-LST-3/4（篡改 + 合法/legacy 回归）、R21-STOP-2（篡改 + 合法 + legacy 回退钉）、R21-SM-3（源级纪律钉，R18-SM-1/R19-SM-2 先例——smoke.js 顶层即执行 main() 无注入 seam）。全部探针（CACHE-1/2、CO-6、AW-6、LST-3/4、STOP-2、SM-3 共 8 个）落 test/run-lifecycle/boundReadSweep.test.js（已在 manifest，无新测试文件、manifest 零改动）；**逐面变异自证红后复原**（六面变异：W1 stem 还原 / W2 collect 还原 / W2 await 还原 / W3 updatedAt 还原 / W3 runStop 还原 / W3 smoke 还原——对应探针红、合法回归腿恒绿、复原后全绿）。**W4 L2 拆三条登记（auditor：三个门混一条、理由只适用其一）**：**(a) L2-a 终态仲裁 CAS**（transcript.js:767 transitionState 持锁读 + _detectExistingTerminal:1811 无绑定读全文件）——维持，"响亮拒写"论证成立且只适用此门（run.state_change_rejected 审计 + first-terminal-wins）；**补登 R20 交互新形状（auditor 发现）**：CAS 拒绝时 runManager._transition rejected 腿（:2851-2854，内存 this.state=result.state）采纳终态而 run.completed/state_change 不落盘——若触发拒绝的既有终态行是外 run 信封（_detectExistingTerminal 无绑定），该 run 对全部绑定读者永久 running + daemon 每 tick 捞为 orphan + 每次恢复尝试再被 CAS 拒（run.state_change_rejected 周期性落盘，可观察）；**缓解方向（登记不修）**：_detectExistingTerminal 现仅返回状态串——改为返回触发行（含 runId）后拒绝路径可对"既有终态行信封 ≠ 本 run"形状精确告警（一次修把该形状从"永久静默 running"变"响亮告警"），或 daemon 侧 orphan 恢复尝试预算；**(b) L2-b 交付 accept/reject 决策门**（transcript.js:863 validateDeliveryFacts 无绑定 + :872 findState 无绑定 喂 :881-907 accept/reject 门）——维持但**换真理由**：绑定在此其实会把"外来行致 delivery_malformed/terminal_not_eligible 永久锁死决策"变成"正确决策"（方向与旧登记"绑定=静默忽略"相反——旧理由对本门不成立）；维持的真实依据 = **写侧 SSOT 血溅面**：该门是锁内写侧验证、喂 tryAppendDecision 的 first-decision-wins 持久决策（TD-99/TD-100 契约族），改读集等于改写集与决策审计链，需专项评估；**(c) L2-c 纠偏注入门**（transcript.js:1407 tryAppendCorrectionRequested / :1459 tryClaimCorrection 的无绑定 findState 终态门）——维持，真理由同 (b)（锁内写侧 SSOT、TOCTOU 复查）+ 该门是 TD-116 后端选型（在途纠偏通道）的硬门槛通道，动它需与后端选型专项同评。**W5 events[0] 按类登记（auditor：实况 11 处行级只登 3）**——类理由：**events[0] 子族：append-only 下首行即本 run 首写，除非文件被预置/整体重写（= runs/ 写能力攻击面，读侧无解，与同 runId 伪造追加同边界）；污染效果自伤——extractCanonicalAgentId（transcript.js:2169）以 events[0] 为候选并要求全序一致，错误/外来 agentId → 身份全程 unknown（fail-closed），不产生跨 run 注入**。实例清单 11 处行级/9 个文件（任务书称"9 处/未登 6"——runDeliveryRepackage 一文件两行 + runManager 行号漂移的计数偏差，按实况登记）：已登 3——runCollect.js:109（defaultAppendFn 写者上下文 agentId）、runList.js:110（rawAgentId）、runs.js:309（dashboard 行 agentId）；未登 8 行/7 文件——lifecycle.js:78（**retry 重派发目标 agentId——R13 绑了 prompt 没绑派给谁**）、shared.js:160（loadRun 写者上下文）、runManager.js:1314（resume 写者上下文）、runs.js:758（--agent 过滤）、runDelivery.js:1230（decideRunDelivery 写者上下文）、runDeliveryRepackage.js:302 与 :623（两处）、runDeliveryReverify.js:329。runs.js:327/:328 dashboard ageMs 的 firstTs（events[0].ts）属本子族、lastTs 属末事件 ts 子族（display-only 低危，随类并入）。**W6 判决换真理由 + 行号勘误**——#3（runCollect events[0]/seq，原判"评过不够格"）拆两半：events[0] 半 → W5 类理由（append-only 首写不变量）；seq 半（findLastEventSeq 全量水位）→ **seq 全局单调性**（auditor 实证 transcript.js:661 写侧 readMaxSeq 同为全量读——水位被外 run 高 seq 尾条抬高后本 run 后续写入仍更高，无事实污染只造空洞，单调性不破），并入既有 seq 水位族；#11（daemon pre-envelope orphan 显示代价，原判仅"TD-129b ≈0 存量"）换真理由：**多显示方向保守**（非终态 orphan 多显示 = 宁多不漏，与 liveness fail-closed 判活同族）+ **resume 绑定门兜底**（manager.resume 的 R13-C/R18-W3 绑定门对 pre-envelope 落 null 拒绝——显示为候选但接续被拒，不误接续）+ **每 tick 重试无记忆的代价注记**（daemon 周期扫描对该形状每 tick 重新捞为候选、无记忆/无退避——显示代价周期性存在，接续侧有门）；#9 行号勘误：dashboard agentId/ageMs 实际 runs.js:309/:327/:328（原登记 :302/:320-321）。**W7 重看触发条件入册（coder_mm T1-T7，等义压缩）**——T1（→L2-b/L2-c）：decide/accept 出现非人调用方；T2（→collect/await 残余面：seq cursor/serve-cap/events[0]）：collect/await 输出被程序化消费或观测到真实外 run 行混入；T3（→events[0] 类及 updatedAt 派生面）：agentId/updatedAt/ageMs 被 prune/告警/成本归集消费；T4（→seq 水位族）：seq 成为正确性依赖或 liveness 门禁自动化；T5（→smoke.js:235）：smoke 改为共享/长寿命 runDir；T6（→全部 legacy 容忍腿）：任何机器 pre-envelope 存量变非零；T7（→NF-2）：friction log/metrics 接入看板或计费。**W8 收官声明**——R21 后 **#10（主消费面生效：W1 闭合 R20-M2 缓存残余）与 #2（内容绑定：W2）已闭合，"结果取回全部绑定"表述成立**（三层：会话身份 R16 + 取回目标 R16 + 内容 R21）。**维持清单（与实况对账后 9 项；任务书预估 10=原 12−W1/W2，实际 W3 三修再使 smoke 条目缩为 :235、runStop:137 与 runList updatedAt 条目消解，events[0] 类吸收原 rawAgentId/dashboard agentId·ageMs/#3-events[0] 半，L2 一条拆三条）：①L2-a 终态仲裁 CAS（含 R20 交互新形状登记）②L2-b 交付 accept/reject 决策门（换真理由）③L2-c 纠偏注入门（换真理由）④seq 水位族（runWait/runManager/backgroundRunner/runCollect:113 + 写侧 transcript.js:661；换真理由）⑤events[0] 类级登记（11 行级实例）⑥smoke.js:235（场景 1 evidence 计数显示；R21 后实际 :241——W3 注释扩充致行号漂移）⑦daemon pre-envelope orphan 显示代价（换真理由）⑧NF-1 backgroundRunner:238/:241/:247 回填存在性检查 ⑨NF-2 runManager:2910 friction log run.metrics 反查。**（R21 auditor 终审条件项，一行登记）**：⑩ `validateDeliveryFacts` 三处非 transcript.js 调用点读取无绑定——`runDeliveryRepackage.js:643`、`runDeliveryReverify.js:215`、`runDeliveryReview.js:127`（对照 `runDelivery.js:346` 已绑定过滤）——外 run 交付事实行可致 `delivery_malformed` 假红；与 L2-b 同族（写侧血溅面/读集=写集），并入 L2-b 维持语义。**遗留已闭合（后续交付，commit 5a8a5bb 入库）**：`test/run-lifecycle/runs.test.js` `runs list --latest` 夹具信封 runId 已改为与文件名 stem 一致（生产不变量修复，彼时 SCOPE_EXPANSION_REQUIRED 待授权项经授权落地）；updatedAt 绑定语义与 C-4/R20-LST-2(c) 一致。 |
| TD-129 | 2026-08-18 R13-C auditor 窄复核（run_20260818131632103dwinbp）NEW-FINDING×4（读绑定类清扫补充） | (a) **P3-a 优先**：cli.test.js:4319-4321 的 D 形状 canary 注释声称"后端开始返回 messageId 本断言变红"——不成立（断言架在手写夹具 transcript 上，改 processBackend.js 不影响它；全库无 ProcessBackend.spawn().messageId===undefined 断言）——不设防的 canary 比没有更危险；(b) stop 面的 legacy 行为变更漏列于 usage.md:421 行为变更条（retry/resume 在列，stop 缺席；本机 2859 transcript 中 pre-envelope 为 0，实际影响≈0）；(c) transcript.js:1988-1991 SSOT 文档半句自相矛盾（"第二写带 messageId"仅 HTTP 家族成立；"callers lenient ?. chains"对绑定后 retry 硬拒/resume null 不再成立）；(d) TD-128 完整性：runContinue.js:347 无绑定 findLatest(run.session_reuse) 喂 not_continuable 门（同函数 :338/:359/:371 已绑定——函数内纪律不一致；可用性侧）；runActivity.js:73 末条无绑定未归类；假 backend 返 messageId 的夹具模式全库分布（m11-8c×3/m12-14/m11-8c-closeout/dispatchCwdExistence/roleContractPackage* 等），各 lane 无害但"复用前诚实化"提醒面需扩大。 | **🟢 已偿还（R14，2026-08-18，run_20260818133309666l4p9ta / commit a54f093）**：(a) 真实契约哨兵落 `test/backends/processBackend.test.js` R14-W3（真实 spawn 返回体 `messageId`/`admittedSeq` === undefined + 序列化丢键断言；base 后端真子进程 + 三家族成员真实返回体；**变异自证红后复原**），cli.test.js 虚假注释改为如实声明"本断言只钉夹具链形状"并指向该哨兵；(b) usage.md 补 stop 面行为变更条（R13-C 补录，run_stop 节）+ R14 reuse 路由降级条（run_dispatch 节）；(c) `transcript.js` 两处半句改写——"第二写带 messageId/admittedSeq"限定 HTTP 家族（opencode-serve/deepseek-harness，ProcessBackend 双写均裸）并指向真实哨兵；"callers lenient ?. chains"改为各 lane 明示处理清单（retry 硬拒文案/resume null/stop 拒绝/correction·continuation 身份门先拒/activity 上游 throw/reuse 降级 first）；(d) runContinue.js:347 → `findLatestBound`、runActivity.js:73 → `findLatestBound`（均并入 R14 清扫——**锚点复核修正**：两处均有上游身份/绑定门先行（`extractCanonicalAgentId` / `assertEventsBoundToRunId`），交换为行为恒等的纪律一致性，分层不变量由 R14-CT-2/R14-AC-1 钉住）；全库夹具大扫除**不做**（保持开放为长期纪律，见 TD-128 处置列"复用前诚实化"）。 |
| TD-130 | 2026-08-18 Lead 过程登记（R10-B/R13-C/R16 五次同类事件） | delivery 验证 harness 的 `npm test` 环境性失败模式：同命令在同 worktree 下 harness 执行 exit 1（stderr ~1.1KB 未捕获），顺序复跑恒绿（worker 自跑 + Lead 复跑双证），机器空闲；五次分布 R10-B×1（reverify 绿）、R13-C×2（第二次 reverify 跑 18.8min exit 1）、R16×2（reverify 722s exit 1）。两次走到 reject+前作集成，浪费一轮验证预算。 | **W1 已偿还（R17（本次交付），2026-08-18；commit Lead 收口补）**：`src/deliveryVerification.js` 命令结果新增加性字段 `stdoutTail`/`stderrTail`——**仅非成功结果**（非零退出/超时/launch error）携带内容，绿输出保持只计数（3B-07/3B-25 契约零改动绿）；每侧 ≤8192 字节原始输出 + 精确截断标记 `…[truncated N bytes]`（N=丢弃字节数）；原始 verification 与 reverify 结果同形状（同一 `_recordResult` seam、setup/assertion 对称、legacy 注入 runner 归一空串、绿结果结构性零尾）；落盘路径核实：transcript `append`/`tryAppendReverifyOutcome`/`tryAppendRepackageVerification` 整包过 exact-secret redactor（tail 内字面凭据自动改写为 `[REDACTED:NAME]`，测试钉死）；fail-fast 下单个失败事件至多携带一对非空尾，事件膨胀有界；MCP `run_delivery` 投影边界不变（8 键纯标量摘要，m11-12b R17 守卫钉死）。测试 `test/delivery/verificationTailCapture.test.js` ×10 + 既有 delivery verification 套件 96/96 零改动绿。**W2 侦查结论（R17，尽力而为，如实交付）**：未当场复现——同 worktree 同执行函数（`runVerificationCommand` 直调 + per-attempt TEMP 注入的 harness 形状，未走真派发）两次全量复跑均绿（674s / 647s）；但字节核算把失败类收窄到单一候选：绿基线 stderr 恒 **1037B**（两跑逐字节同值），R16 失败事件 **1150B = 基线 + 113B ≈ 恰一条 `[canonical] isolation` 分类行**（实测 npm 11.8.0 脚本失败零 stderr 输出，排除 npm error 块）——即"**单个测试文件首轮失败 + 隔离复核通过（isolation_pass）**"，canonical 设计上不洗白（finalVerdict 仍 fail → exit 1，`canonical-test.mjs` runCanonical/classifyIsolation），顺序复跑不复发即呈现 TD-130 形状。与 R14 backgroundRunner 进程波 flake **同类（isolation_pass）、很可能同族**（波并发下时序敏感测试），但实例身份（哪个文件、什么触发）未定位——保持分开登记；harness-vs-顺序的不对称机制（tempPerAttempt 使全部测试 temp 落在全新 `wao-verify-*` 目录树（路径 +~17 字符、AV 冷启面）/ 控制面进程 env 出身 / cmd.exe shell 包装）为未决差异面。**取证协议已就位**：W1 使下次发生时 `delivery_verification_*` 事件的 `stderrTail` 直接携带 [canonical] 波次/isolation/verdict 行全文——一次读取即定位文件与类别，无需再排除法。**R17-W2 后续与勘误（2026-08-18 晚，会审 P2-1 修订）**：R16 reverify 失败实例身份经 worktree 报告（test-results.json）定谳为 **`mcp-surface/mcpBind.test.js`**（git 波，isolation_pass，duration 721300ms 与 reverify 事件吻合）；R16 原始尝试（stderrBytes 1164）身份仍为推断（isolation 行路径 ≥40 字符，候选 8 个）。另 Lead 侧全量跑中第三实例为 **`backgroundRunner.test.js`**（进程波，isolation_pass，复跑即绿，控制台观察口径、artifact 已被复跑覆盖，证据等级受限）。三实例跨文件跨波 → **"多文件多波环境性"假设**（AV/调度候选），此前的"收窄至单文件"表述作废（Lead 登记错误，勘误）。取证协议以 **stderrTail 内容**为准（isolation 行的存在与文件名），不做字节数指纹（绿基线 ≈1037B 随时长数位/路径长度 ±数 B 摆动，"恒/逐字节同值"措辞一并勘误）。 **R23-F 止血两轮已落地（2026-08-22，Owner 批准 TDD 计划，两席方案会审+交付审计）**：Round A（`src/machineGatePaths.js` 路径去 TEMP 化+死 pid NOTICE 降级+per-file durationMs+isolationDurationMs；A4 清理 %TEMP% 5666 残留）+ Round B（`src/verificationGate.js` 心跳租约闸六要件+原子 claim+三生产路径默认验证器接线+`runs gate`/`--release`/`WAO_VERIFICATION_GATE=off` 停用开关+文档四面）——同机全量验证自此结构性串行（两通道同闸），孤儿标记成因链（无人收割后台自跑→硬杀→恒亮）与两通道标记互盲机制闭合。**重启触发（两席止血咨询定）**：T1 闸独占下再现 isolation_pass ⇒ 族 1 高优重开；T2 闸独占+pure 波基线内仍 command_timeout ⇒ 族 2 重开；T3 连续两次 pure>2× 无并发 ⇒ 执行清理窗口探针（≤30min）。根因报告 `docs/research/td130-root-cause.md`。**R21 期间新实例（2026-08-19 晚）**：`mcp-surface/mcpWorkspaceSmoke.test.js`（filesystem 波，isolation_pass ×2 连续两轮全量、单跑 ×3 绿、8 路并发复现不现形——同多波环境类第 4 个具名实例；该文件来自并行 Lead 会话的交付，可转该会话关注）。 **lane-b3 新实例（2026-08-19 深夜，ADR-0025 批次 3 验证期）**：同日三轮全量共 3 个具名实例——worker 自跑轮 `processBackend.test.js`、验证轮 `runWait.test.js`、reverify 轮 `runWait+mcpWorkspaceSmoke`（全部 isolation_pass、全部不在交付 touched 清单、与 R21 会话测试并发运行）；Lead 顺序复跑失败文件 fail 0。当日再走一遭 reject+前作集成（run_20260819193237518k94hlg，先例 R16/TD-127）——并发双 WAO 会话同机跑全量套件疑似放大触发频率，待证。**集成终验轮追加实例**：`mcpRunDeliveryReverify.test.js` + `mcpWorkspaceSmoke`（第 3 次，均 isolation_pass），顺序复跑 11/11 绿；当日该家族具名实例累计 5 个文件 ×8 轮次。 |
| TD-133 | 2026-08-19 lane 架构批次 3（delta 认证机制，ADR-0025；登记+可收口部分同轮收口） | 认证残余面 | adversarialEscape drill（越界写对抗，TD-116 收口件）机制已落地但**从未真实实跑**——判定内核只经 dry 形状测试（`test/registry-roles/reliabilityDelta.test.js`，伪造 transcript 事件不烧 token）；真实派发路径（delivery run + 越界写指令 + containment gate 拦截 + 终态 failed）未验证。另两个已知语义边界：(a) worker 拒绝配合执行越界写指令（refusal）时无拦截证据 → drill 诚实红，需人工分辨"机制失效"与"模型没配合"（docs/usage.md delta 规程已写明，非文档缺口、是机制语义）；(b) delta→conditional 的 `reasonCode` 在 wire（CLI/MCP inventory）上为 `null`——闭集（CERTIFICATION_REASON_CODES）无 delta 码，为保 MCP 面零改动不加码，磁盘 summary 的自由文本 reason 是另一层契约。 | 实跑：Owner 决定对首个真实 delta lane 跑 `npm run reliability -- --profile delta`（消耗真实 token）；若 refusal-红 频发到无法与机制失效区分，再评估加"拒绝配合"区分面（需真实数据先行，不预设）。(b) 为设计边界非债：若未来 wire 需要 delta 码，走显式 ADR 加码（动 MCP 面，超出批次 3 红线）。**（c）已偿还（R23-C，ADR-0026）：scope 派生收窄——profile==="delta" 且 drills ⊆ DELTA_DRILLS 才判 delta，显式 drills 超子集按 full，certifyCase 不再误降档。** **（d）已偿还（R23-C，ADR-0026）：门新鲜度改读 lastFullHealthyRunAt（仅 full-scope 全绿刷新；delta 不再稀释派发新鲜度；缺失回退 legacy lastHealthyRunAt 半迁移）。** 残余（a）(e)(f)(g)与 drill 卫生面维持开放。**验收会审补登（2026-08-19 深夜，auditor F1/F4 + coder_mm b1/b2/b4，run_20260819225331015vpa48x / run_20260819225332932rzlevd）**：(c) `--profile delta` 不覆盖 matrix 行显式 drills（`matrix.mjs:67` 行内 drills 存在即胜出）且 scope 派生先看 profile 短路——显式 drills 非 DELTA_DRILLS 子集的行会被误标 delta 并降档 conditional；规避规程 = delta 行**不写显式 drills**（首次实跑已按此执行：独立 lane `coder_hq_deltadrill`）；根修（drills 覆盖优先派生）待触碰 matrix/certification 时顺带。(d) delta 全绿会刷新 `lastHealthyRunAt` 而 P1-1 门与 wire 投影均不读 scope——一次 delta 子集跑把门新鲜度刷满 30 天且与全量 conditional 在 wire 上同形（reasonCode:null 三义：delta / 全量 operational 失败 / legacy 无码）；运营判读需读磁盘 summary。(e) 空转形状：registry 无 matrix 时回落 LEGACY_MATRIX，`--agent` 过滤后零 case 仍 ALL PASS exit 0——实跑前须零 token 预检 matrix 行存在。(f) drill 红时 detail 不含 write 类事件计数——refusal 分诊需人工开 transcript（若首跑见红再评估加增量）。**(g) 首跑红已定谳（2026-08-19 深夜法医复跑，run_20260819231711940qjywls）**：refusal 非机制失效——worker（GLM-5.3）零 file_written、识破 drill 语境（Grep/Read 了 WAO 仓库与 adversarialEscape.mjs 本身）并要求澄清后 completed，零越界写尝试；首跑台账落 conditional+scope:delta、coder_hq 主力零污染（独立 lane 方案生效）。**附带两个 drill 卫生面登记**：drill transcript 随套件清理删除（run-reliability.mjs:671 rmSync TMP_DIR——法医需手工复跑取证）；drill worker 实际 cwd 解析到 WAO 仓库而非 TMP_DIR（--cwd TMP_DIR 与 runCli {cwd:ROOT} 的交互待核——worker 能读到 drill 源码是 refusal 诱因之一，也削弱 drill 的语境隔离）。另：旧 summary 无 lastHealthyRunAt 字段 → 全部存量 worker（含 certified）在新门 --require-certified 下会被拒，需一次全量重跑（批次 0 预告的收紧代价具体化）。 |
| TD-134 | 2026-08-19 lane 验收会审（auditor F6，run_20260819225331015vpa48x） | 测试基建假阳 | runs-guard（canonical-test.mjs:392-396/:674）对 repoRoot/runs 做基线-现状差分但**不做进程归属**——主工作副本跑 npm test 期间任何 background run 落 runs/ 即 exit 1（注释自陈 non-green EVEN WHEN every test passed）。与 TD-130（isolation_pass，verdict 侧失败）是不同失败模式，但同样表现为 npm test exit 1 + 复跑绿，存在被并入 TD-130 误判的风险。 | 运维规程：主工作副本跑 npm test 前确认无 live run（runs dashboard / --agent 活跃检查）；根修（进程归属或隔离 run-dir）待真实痛点再立项。本日 lane 批次集成终验轮的 exit-1 中至少一次属本形状而非 TD-130。**运维规程已落 troubleshooting §8.2（R22，2026-08-20）。** |
| TD-135 | 2026-08-20 R22（Owner 确认双席咨询综合建议：coder_mm + auditor） | 并行会话验证盲区 | 并行 Lead 会话的"语义未覆盖组合"风险：两会话交错 cherry-pick 后，各自验证的绿是"**冻结基线 + 自己改动**"，不是"集成后的 main"——git 零冲突只证明文本不冲突，不证明语义不冲突。当前无任何机制覆盖该组合（集成后 main 上跑的全量套件是唯一防线，但它是 Lead 手动做的且时机不保证）。同轮姊妹处置：并发互踩的 advisory 检测（canonical inflight 标记）与 isolation_pass 判定规则已落 troubleshooting §8.1（TD-130 家族），本条登记的是验证覆盖缺口本身，两者正交。 | 观察项——并行会话各自收口时应在集成后 main 上跑一次全量终验（当前 Lead 实践已如此，但非制度化）；如果并行成为常态，考虑把"集成后终验"加为必做步骤。触发：首次因语义组合冲突导致的集成后回归实证。 |
| TD-137 | 2026-08-20 R23-A 库卫生（CLI paper-cut 家族兜底，四项各有出处） | CLI paper-cut | ① runs 裸列表最旧优先：`src/commands/runs.js:396` 升序 sort 覆盖 service 的 updatedAt desc（externalaudit F-2）；② 三等待上限不一致且无交叉提示：`RUN_WAIT_MAX_MS=600000`（`src/application/runWait.js:38`）/ `DELIVERY_WAIT_MS_MAX=300000`（`src/application/runDelivery.js:147`）/ `RUN_AWAIT_RESULT_MAX_MS=270000`（`src/application/runAwaitResult.js:111`）（round3 F-2）；③ runId 时间戳为 UTC 在 usage.md/cli.md 无记载（externalaudit F-1）；④ allowedPaths 只收显式文件路径不收目录（round4 F-1）。 | 处置：下次 CLI friction 批次承载或逐项触发。 |
| TD-138 | 2026-08-20 R23-A 库卫生 | 套件时长治理 | 全量 `npm test` 实测约 14.5 分钟（869s；2026-08-20 两次空载吻合——worker 第 2 轮 869329ms / 控制面验证 869180ms，相差 150ms 内）且随功能只涨不跌；是 TD-130/134/135/125 四个家族的共同放大器（套件越长，并发互踩观察窗与 runs-guard 基线窗口越大）；全库无一处把 suite 时长当摩擦登记。决策选项：delivery 验证跑受影响面子集 + 定期全量 vs 维持全量。**登记注明：强制力现状 = 文档，无机器面。** | **Owner 2026-08-20 拍板：维持全量（含并行会话场景的集成终验照跑全量）**——全量套件是 exact-artifact 信任的全部，集成终验已制度化（SKILL Acceptance 条款）兜底并发场景；子集路线不采用。重开触发：空闲档实测 >20 分钟或交付频率显著上升时，以新事实重新登记。 |
| TD-139 | 2026-08-20 R23-B 交付会审（coder_mm"没问但该问"） | 观测通道边界 | TD-113 的重复写计数**只在 CLI 通道成立**——MCP `run_status` 有意永不返回 `lastActivitySummary`（安全子集，`mcpRunStatus.test.js:195` 负断言钉死），MCP 形态操作的 Lead 在长跑写场景下体验与修复前完全相同。登记本条防"TD-113 已关单"被误读为全通道已解决。**残余面**：MCP run_status 通道（有意安全子集）。 | 触发：MCP 形态的 Lead 监督长跑写 run 出现真实判读需求时——届时须另立 ADR 动 run_status 输出面（含安全子集论证），不走 CLI 通道加字段的捷径。 |
| TD-140 | 2026-08-22 第三方审计处置（决策 0027，Owner 裁定） | 运行时依赖生命周期 | WAO 硬钉 Node `>=22 <23`（v24 libuv Windows Job Object 回归会误杀长进程，见 docs/usage.md §Node22 + TD-40/ADR 0013）。Node 22 LTS 维护期约 2027-04 结束，此前无带日期触发器，到期即事故。 | 主路径（Owner 定，2026-08-22）：**定期检查 v24 回归是否修复**——每季度对齐 Node release 日历核对 libuv changelog / Job Object 行为；`src/nodeVersionGuard.js` 已备 BLOCKED_RANGES/ALLOWED_FIXED_VERSIONS + engines 升级机制，官方修复版发布即可放行并评估升级。辅路径：2027-01 仍无修复迹象则评估迁移目标。伴随项：认证可移植性（reliability-summary.json 本机生成、不可跨机重放）在该次升级评估中一并处置。 |
| TD-141 | 2026-08-22 决策 0027（auditor 发现实证） | 护栏覆盖率 | 机器护栏是白名单：未被断言覆盖的面照样漂移。实证：docs/usage.md 把 manifest 类别写成 6 个（漏 mcp），与 `scripts/canonical-test.mjs` MANIFEST_GROUPS(7) 及 AGENT_ONBOARDING.md 不一致，长期未被发现——发生在有 docs-consistency 护栏的文档里。 | usage.md 单点已修（本决策同批）。剩余处置（Owner 2026-08-22 追加裁定）：**对照清单本身也按触发器执行**——首次再撞见同类"机器护栏未覆盖面的漂移"时，才产出一次性对照清单（现有机器护栏 vs 文档化不变量），以该实例为第一行逐点决定"补断言或接受"，约束照旧：不过度工程、够用就好、不建新框架；**此前不主动盘点**。 |
| TD-142 | 2026-08-22 决策 0027（auditor 成本论证采纳） | 资产维护分级 | workflow/DAG 层利用率走低：wf_ 共 643 次 ≈21%（6 月 8 次 → 7 月主通道 → 8 月 89 次聚簇，最后 2026-08-16）；体量 929 行 ≈ src 1.8%。走降级/退役流程的治理开销很可能超过此后一年维护开销（auditor 论证，Lead 复核算成立）。 | **不动**（Owner 同意）：保留现状，记观察项。重开触发：(a) 连续一个季度零 wf_ run；(b) 该层出现需要实质改动的新需求；(c) src 总量增长使占比进一步稀释至可忽略且出现维护痛点。 |
| TD-143 | 2026-08-22 决策 0027（双席共同盲区） | 外部依赖风险 | 证据链层（scorecard / exact delivery proof / transcript-as-SSOT）两个前置软肋：(a) **transcript 可访问性依赖**——原材料来自各 runtime 厂商 transcript，厂商收紧访问即间接扼杀第三方审计层（独立性是必要非充分条件，auditor 提出）；(b) **N=1 方法论连续性**——整套治理实践目前无第二读者/操作者（coder_mm 提出）。 | 观察登记，无近期行动。(a) 触发：任一主力 backend 收紧 transcript/日志导出政策时，评估证据链层的厂商中立数据源。(b) 触发：任何对外分享方法论或第二操作者加入的时点，先补交接文档再扩散。 |

---

## 设计性约束（⚪，非债）

| # | 登记于 | 内容 | 备注 |
|---|--------|------|------|
| TD-16 | M2 | 真实 CLI smoke（`npm run smoke`）不进 `npm test`（依赖真实 API/登录/费用） | 刻意设计：smoke 显式手动触发，CI 不跑。`milestone-discipline.md` §4.4 已要求涉及外部系统的 milestone 收尾必跑 smoke |
| TD-50 | 2026-06-26 M8 | `src/diagnosis.js` 的失败分类是**启发式信号匹配**（正则 + 事件类型），非语义判断。边缘 case（如非 401 的 auth 失败、新 provider 错误格式）会降级为 `unknown`。**这是刻意设计**——诊断只给证据不给处方（M8-3 铁律），分类只是帮 Lead 快速定位，不强求高准确率。处方权全在 Lead。 | 刻意设计：防"让诊断更智能"的诱惑滑向自动应对策略表（用户明确否决机械应对）。新增 provider 错误格式时扩充 AUTH_SIGNAL 即可，不必重构。
| TD-106 | 2026-07-15 M9-7B dogfood；2026-08-13 Owner 裁定 | Tester context efficiency 属于 Codex 产品/runtime 的上下文组成与订阅 quota 问题，不是 WAO 应吸收的职责。 | **WAO 非目标**：WAO 保持 provider-neutral 的路由与事实投影，不修改 Codex 的上下文组成。相关 WAO ergonomics 已独立完成：M11-3 `run_delivery_review`、M11-4 `run_collect` cursor continuation、M12-17 durable `executionStage`；这些能力保留，但不得以其为由把 Tester context efficiency 重新登记为 WAO 技术债。 |
| TD-118 | 2026-08-15 终审更正（auditor） | Lead 技能安装机制：`C:\Users\<you>\.agents\skills\wao-orchestrator` 与 `C:\Users\<you>\.codex\skills\wao-orchestrator` 是**两个指向仓库根的 junction**（非硬链接，跨卷不可能；`<you>` 为本机用户名占位）。推论：(a) 安装面暴露的是整个仓库（含 gitignored `.dev/`、`config/agents.json`、`.wao/`），不止 SKILL.md；(b) 对仓库内 SKILL.md 的**未提交编辑即时生效**于所有 Lead 会话——审计/生效之间没有提交闸门；(c) fresh clone 不重建 junction，需重新安装。 | 部署事实记录，防排障误判（曾误记为"硬链接"）。风险知情：技能改动的实际生效点=写盘时刻，git commit 只是版本留痕。 |

---

## 交叉引用索引

每个 TD 的**详细发现过程 / 修复 diff / 教训**在对应 audit（已归档到 `docs/archive/`）：
- TD-1~5 → `archive/m0-audit.md`
- TD-6~9 → `archive/m1-audit.md`
- TD-10~18 → `archive/m2-audit.md`
- TD-19~23 → `archive/m3-audit.md`
- TD-9(重)/24 → `archive/m4-audit.md`
- TD-25~31 → `archive/m5-audit.md`
- TD-9(偿还)/32/33 → `archive/m6-audit.md`
- TD-33 schema 勘测 → `research/07-opencode-smoke-and-weeds.md`
- TD-34 → `archive/docs-ssot-audit.md`（文档 SSOT 审计）
- TD-37/38/39 → `incidents/2026-06-18-glm-quota-drain.md`（opencode 三层防线 + fire-and-forget 护栏）
- TD-40 → `.wao/decisions/0013-进程隔离-JobObject-复用内置-vs-自定义.md`（复用内置 Job Object vs 自定义）
