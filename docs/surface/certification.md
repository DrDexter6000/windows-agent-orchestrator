<!-- GENERATED by scripts/gen-certification.mjs — DO NOT EDIT; run npm run gen:certification -->
# WAO backend 能力与配置表达力（认证事实生成层，TD-162）

Audience: repository-level agents and humans; 认证台账 live 值不在本文件（见 §三）。

数据源（现成 SSOT，本文件不含第二真相）：

- 六轴能力：`src/registry.js` `KNOWN_BACKENDS` × `src/backends/factory.js` `BACKEND_CAPABILITY_AXES` / `readBackendCapabilities`（strict `=== true`——未声明读 false，fail-closed）。
- 配置表达力四轴：对经共享工厂构造的 backend 实例调用 `validateAgentPolicy` 的**行为探针**派生（判定法沿用退役 TD-162 policy 门守卫的同一套派生法；kimi 档位绑定模型提取自 `src/backends/kimiCode.js` `KIMI_K3_MODEL_ID` 源常量）。
- 每 backend 条件与限制说明：`src/backends/factory.js` `CAPABILITY_NOTES`（单一出处，§二之末逐条渲染）。

漂移纪律：判定词与档位集合**只在生成期从代码派生**；磁盘副本由字节钉守卫（`npm run gen:certification -- --check` + `test/isolation-infra/docsSurface.test.js` + `test/isolation-infra/docs-consistency.test.js` 的 TD-162 生成物守卫）钉住。**守卫的实际保证是「换行归一化后文本一致」**（CRLF/LF 视为相同——`.gitattributes` 已钉 `docs/surface/*.md eol=lf`），不是原始字节相等；入口检测失败时 `--check` fail-closed（exit 1），绝不静默跳过——改 backend 代码/`CAPABILITY_NOTES` 后须 `npm run gen:certification` 再生成并提交。

## 一、六轴能力闭集声明（strict === true）

| backend | supportsRoleContract | supportsSessionReuse | supportsInFlightCorrection | replayByRespawn | reportsTokenUsage | reportsCommandExitCode |
| --- | --- | --- | --- | --- | --- | --- |
| opencode-serve | ✅ | — | — | — | ✅ | ✅ |
| claude-code | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| codex | ✅ | ✅ | — | ✅ | ✅ | ✅ |
| kimi-code | ✅ | ✅ | — | ✅ | — | ✅ |
| deepseek-harness | ✅ | — | — | ✅ | ✅ | ✅ |
| deepseek-acp | ✅ | ✅ | — | — | — | — |

✅ = 类声明 `=== true`；— = **未声明或非严格 true**（strict `=== true` 读取为 false——两者同格，例如 deepseek-acp 多轴是显式声明 `false`）。带条件/限制的轴（如 opencode-serve 角色合同的版本门）见 §二条件与限制说明。

## 二、配置表达力四轴判定（validateAgentPolicy 行为探针派生）

判定词（只管 **WAO 接线层**）：**支持**＝全部探针通过；**不支持**＝全部探针被 `validateAgentPolicy` 拒绝（fail-closed 硬拒，不是静默忽略——成因三分：上游无此能力 / 上游有原语但 WAO 未接线 / 配置形状不一致被拒，逐条事实见 `docs/usage.md`「上游 harness 原语对照（实测）」节）；**条件**＝部分探针通过（条件写在格内，探针派生）。

| backend | model override（model.id / --model） | reasoning effort | model.contextWindow | provider 块 |
| --- | --- | --- | --- | --- |
| opencode-serve | 条件：仅 OpenCode 形状 `{providerID, id}` | 不支持 | 不支持 | 不支持 |
| claude-code | 支持 | 支持 | 条件：仅 provider 路径 | 支持 |
| codex | 支持 | 支持 | 不支持 | 不支持 |
| kimi-code | 支持 | 条件：effort ∈ {low, high, max}，须绑定 kimi-code/k3 | 不支持 | 不支持 |
| deepseek-harness | 支持 | 条件：effort ∈ {high, max} | 支持 | 不支持 |
| deepseek-acp | 不支持 | 条件：effort ∈ {low, high, max} | 不支持 | 不支持 |

衔接：per-dispatch `--model` 只替换 `model.id`、兄弟字段保留——opencode-serve 的 agent 必须先带 `providerID`；`--reasoning` 的六值闭集（minimal/low/medium/high/xhigh/max）由 `registry.js` `REASONING_EFFORTS` 在 registry 层校验，backend 层再按上表条件格收窄。

### 条件与限制说明（CAPABILITY_NOTES 单一出处：src/backends/factory.js）

- **opencode-serve**
  - model override：须 OpenCode 形状 `{providerID, id, variant}`；canonical 裸 `{id}` 被拒（模型路由由 model.providerID 承担）
  - provider 块：不支持 provider 块——模型路由由 `model.providerID` 承担
  - supportsRoleContract（角色合同）：须 serve healthy 且版本 ≥ 1.18.0（派发前运行时探测，OPENCODE_NATIVE_SYSTEM_MIN_VERSION）
  - reportsTokenUsage（token usage）：session.tokens 周期轮询
- **claude-code**
  - model.contextWindow：仅 provider 路径（wrapper `--context-window`）；native OAuth 直连被拒
  - provider 块：经 provider wrapper（`baseUrl` / `apiKeyEnv`）表达
  - supportsRoleContract（角色合同）：`--append-system-prompt`（内容直传，恰好一次）
  - supportsSessionReuse（会话复用）：`--session-id` / `--resume`（opaque uuid 由控制平面派生）
  - supportsInFlightCorrection（在途纠偏）：stdin stream-json 排队（同一活进程；delivered 证明字节被接受，不证明模型执行了该轮）
  - reportsTokenUsage（token usage）：result 帧 usage
- **codex**
  - model.contextWindow：无 CLI flag 可表达，配了即拒
  - provider 块：codex 自有登录（非 anthropic-compatible wrapper），配了即拒
  - supportsRoleContract（角色合同）：`-c developer_instructions` 追加（TOML 安全转义；不替换 base instructions）
  - supportsSessionReuse（会话复用）：`codex exec resume <thread_id>`；id 由 codex 自产（thread.started.thread_id）、runner 运行期补记入 `session.created`；resume 轮 id 缺失即派发前拒绝
  - reportsTokenUsage（token usage）：turn.completed 帧 usage
- **kimi-code**
  - reasoning effort：effort 编译为 `KIMI_MODEL_THINKING_EFFORT` 子进程 env；`agent.env` 自设同名被拒；档位与模型绑定见判定表（探针派生）
  - provider 块：kimi 托管认证，配了即拒
  - supportsRoleContract（角色合同）：拼进同一条 prompt（非系统级通道，prompt 级引导）
  - supportsSessionReuse（会话复用）：`kimi -r <session_id>`；id 来自轮末 `session.resume_hint`、runner 运行期补记；resume 轮 id 缺失即派发前拒绝
  - reportsTokenUsage（token usage）：stream-json 无 usage——tokenBudget 不生效（TD-87）
- **deepseek-harness**
  - reasoning effort：可省略（未配置不拒）
  - provider 块：组合由 `dshConfigPath` / `dshProvider` 表达，配 provider 块即拒
  - supportsRoleContract（角色合同）：`DSH_SYSTEM_PROMPT`
  - reportsTokenUsage（token usage）：assistant/message 帧 usage
- **deepseek-acp**
  - model override：模型经 shipped acp profile 的 session configOptions 承载；通道已证可 set（Phase 5），但 WAO 未接线（ACP 值形状是 provider/model JSON 对，非裸 model.id）；配了即拒，模型取 profile 缺省
  - reasoning effort：经 `session/set_config_option` 下发（六值闭集 ∩ ACP 广告 off/low/high/max 的交集，不发明映射）；响应未确认即 fail-closed 拒绝派发；resume 轮不发 set，改用 session/resume 响应 configOptions 只读核对，不符即拒
  - model.contextWindow：同 model 块——无可验证设置通道，配了即拒
  - provider 块：组合面固定为 `--profile acp` + 操作员 patch，配 provider 块即拒
  - supportsRoleContract（角色合同）：per-dispatch `--patch` personaPrefix（结构化序列化）
  - supportsSessionReuse（会话复用）：ADR-0031 §3.6 关联面：resume 信封只携带前任 WAO runId，sessionId 由 spawn 权威按 runId 从转录取回、in-process 送达（不进 argv）；关联缺失/损坏/上游拒绝一律 fail-closed 拒绝，绝不静默新会话；仅 stable-workspace lane 的非 delivery 派发，delivery 一律 fresh
  - reportsTokenUsage（token usage）：`PromptResponse.usage` 实测可为 null——声明 false（2026-09-20 裁定）；翻转条件 = 有可验证 token 计量通道

## 三、认证台账（指针；严禁嵌入 live 值）

- **组件层台账**：`runs/component-checks.json`（gitignored 运行时状态）——backend/llm 单独验证记录（组件键含 runtimeIdentity 指纹，版本漂移降 advisory）。刷新：`npm run component-check`。
- **组合层台账**：`runs/reliability-summary.json`（gitignored 运行时状态）——按席位 agentId 的 certified/conditional/draft-only 台账。刷新：`npm run reliability`。
- 台账是**运行时状态**，由上述命令产出与刷新；本生成文件只承载静态能力/配置表达力事实（§一/§二）——嵌入台账 live 值会把它变成会过期的快照，禁止。
- 认证检查结果五态判定纪律（pass / fail / not-applicable / blocked / inconclusive）与能力轴分层：`docs/usage.md`（ADR-0032 §8 节）；两层认证决策：`.wao/decisions/0032-两层验证与认证.md`。
